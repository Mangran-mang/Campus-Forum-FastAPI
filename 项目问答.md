# 项目问答：评论 / 图片上传 / 私信 模块自测（2026-08-06）

> 说明：Q1-Q23 为提问形式的学习自测题。每题包含「问题 → 我的答案 → 纠正/参考答案」。
> 我的答案保持原文不变，纠正部分是批改后的正确理解。

---

## 第一组：评论模块

### Q1. `model_comments.py` 里 `parent_id` 是自引用外键。请解释：`parent` 和 `replies` 两个 relationship 各自表示什么方向？为什么 `parent` 需要写 `remote_side="Comments.id"` 而 `replies` 不需要？

**我的答案：**
parent和replies都是表的自连接后映射本表的内容，parent父id映射replies，replies子id映射parent；首先parent和replies本质就是id，所以remote_side赋值也要是id，parent需要写remote_side是要说明被连接的id是远端，而现在这个表是正在操作的表，既然parent已经说明了情况，那replies就不需要再写了，当然写了也不错

**纠正：** ✅ 核心正确。
- `parent`：向上找父评论（本地 `parent_id` = 远端 `id`）
- `replies`：向下找子回复（远端 `parent_id` = 本地 `id`）
- `remote_side` 的意义：自引用时同一张表 JOIN 自己，SQLAlchemy 分不清哪个 `id` 在"远端"，`remote_side="Comments.id"` 明确告诉它 `id` 是远端列。`replies` 因 `back_populates="parent"` 已绑定同一对关系，且 `parent` 侧已声明 remote_side，故可自动推导、无需再写（写了也不报错）。

---

### Q2. 评论的 `content` 用 `String(255)`，帖子的 `content` 用 `Text`。这是刻意的吗？你如何设计，为什么？

**我的答案：**
帖子模块是我写的，我用Text的本意是觉得贴主发帖时的内容可以发更多，这是作为帖子的特征，而评论用String(255)完全足够了，不够的话再多发两条评论即可，但帖子多发两篇就是换了一个帖子了，如果说是贴主自己发评论也行，但Text更能一次性满足需求

**纠正：** ✅ 答得非常好。
- 帖子 = 一个"实体"，内容可能很长 → `Text`（长文本，不限长度）
- 评论 = 挂在帖子下的一"行"，255 字符足够表达，且评论可以追加多条
- 这是有设计意识的判断：**字段类型匹配业务粒度**。

---

### Q3. `crud_add_new_comment_into_post` 里发评论前做了什么校验？如果前端传了"属于另一个帖子的 parent_id"会怎样？

**我的答案：**
crud_add_new_comment_into_post的第一层父评论校验里嵌套了一个crud_get_comment_by_comment_id函数，它是通过评论id找一条评论，这里参数给的是评论的父评论id，也就是说如果评论有父评论，那么就会去找父评论，然后检查这个父评论所属的帖子是不是目标帖子（或者说当前帖子）的评论，如果否，则抛出错误

**纠正：** ✅ 正确。这段校验防止**跨帖子楼中楼**——拿别的帖子的评论 id 当 `parent_id` 会造成逻辑错乱，所以报 400。

---

### Q4. `db.add(orm_comment)` → `db.flush()` → `crud_add_experience` → `db.commit()`。请解释 `flush()` 和 `commit()` 的区别，以及为什么发评论要先 flush 才能加经验？

**我的答案：**
commit包含了flush，flush是发送sql语句然后执行，commit做最后一关，执行后即永久保存，

先发送评论还是先加经验，效果其实一样，但这样逻辑上更加通畅，只是crud_add_experience（）只是加经验但不提交事务，所以它也要等commit，于是方便起见就一起commit了，所以你先加经验，再去添加这条评论，就不需要这个flush了

**纠正：** ⚠️ 前半对，最后一句"先加经验再添加评论就不需要 flush"是**错的**。
- `flush()`：把已 `db.add()` 的对象**立即发送 SQL 执行**（但未提交，事务还在）
- `commit()`：提交事务，永久生效（commit 内部会先自动 flush）
- 这里 flush 的真正目的：让 INSERT **先于**经验 UPDATE 真正落库，形成确定的事务内顺序，同时让 `orm_comment.id` 自增值可用；顺序与"先加经验"无关
- `crud_add_experience` 内部只发 UPDATE 不 commit（由调用方统一 commit），所以必须等最终的 `commit()` 一起生效
- **正确理解**：顺序无所谓，flush 的意义是"尽早发 SQL + 拿自增 id"，不是"为了顺序"

---

### Q5. `crud_get_comments_by_post_id` 只查一级评论（`parent_id IS NULL`），楼中楼怎么被带出来的？说出 selectinload 链式加载的完整路径。

**我的答案：**
options是告诉sqlalchemy要去查关联的属性，而selectinload是查询的方式，crud_get_comments_by_post_id里的options有两个关联加载，/post/Comments/Comments.author和/post/Comments/Comments.replies/Comments.author

**纠正：** ⚠️ 方向对，但缺关键一环。完整逻辑：

```python
stmt = (
    select(Comments)                              # ① 只查一级评论
    .where(Comments.post_id == post_id, Comments.parent_id.is_(None))
    .options(
        selectinload(Comments.author),            # ② 一级评论的作者
        selectinload(Comments.replies)            # ③ 一级评论的子回复们
            .selectinload(Comments.author),       # ④ 每个子回复的作者
    )
)
```

执行后 SQLAlchemy 生成 3 条 SQL：
```
1. SELECT * FROM comments WHERE post_id=? AND parent_id IS NULL   （一级评论）
2. SELECT * FROM comments WHERE parent_id IN (一级评论的id们)      （所有子回复，批量）
3. SELECT * FROM user WHERE uid IN (所有评论者的uid)              （所有作者）
```
**关键点**：子回复是第 2 条用 `IN` 批量捞出来的，然后自动挂到 `comment.replies` 上。

---

### Q6. 删除评论用 `delete(Comments).where(Comments.id == comment_id)`，而不是先查再 `db.delete()`。两者区别？`result.rowcount > 0` 判断什么？

**我的答案：**
因为前面已经查过评论存不存在了，所以后面可以直接delete。先查后删或写到一行，没有本质区别，最后的return是返回的bool结果，如果结果>0就是True，说明删除成功，否则就是false

**纠正：** ✅ 基本正确。补充：先查再删多一次查询（但能拿到对象引用/触发 ORM 级联），直接 `delete()` 语句是"一条 SQL 删到底"；`rowcount` 是数据库返回的"受影响行数"，>0 说明真的删到了。

---

### Q7. `addcomment` 的通知逻辑里：什么时候通知"帖子作者"？什么时候通知"父评论作者"？

**我的答案：**
分支就是看评论有没有父评论，有父评论了说明当前用户是评论在一级评论下的，理应通知父评论作者，否则通知贴主

**纠正：** ✅ 分支判断对了，但**漏了一个隐藏条件**：两个分支外面还有 `if post and post.author_uid != commenter_uid`（以及父评论分支里的 `parent_comment.author_uid != commenter_uid`）——**不能通知自己**。自己评论自己的帖子/回复自己的评论时，不发通知。

---

### Q8. 通知的创建在 router 层调用而非 crud 层，结合分层原则，这有什么问题？

**我的答案：**
其实业务层应该是专门的一层，只不过我们项目不大，和crud层合并了，如果分层不清晰，会让项目耦合程度变高，后期维护成本和压力会更大，你说的应该在crud层调用，意思是应该在评论模块的crud里吧，那样的话确实更好，但我们项目功能的拓展到这里基本就结束了，所以不改也问题不大

**纠正：** ✅ 判断成熟。业务规则（"谁触发通知、通知谁"）应在 crud 层，router 层只负责"接口外观"。对当前规模这是可接受的取舍，能意识到这个问题本身就是价值。

---

## 第二组：图片上传模块

### Q9. `images` 表用 `target_type` + `target_id` 关联"帖子或商品"但没有外键约束。这种设计叫什么？好处和隐患？

**我的答案：**
这里直接用str类型的target_type和str的target_id来表明是谁的图片，这样非常灵活，缺点就是它是str，写成不存在的类或id也不会报错，我感觉用枚举更合适，

**纠正：** ✅ 非常棒，还自己提出了"枚举"方案。补充：
- 这种设计叫**多态关联**（polymorphic association）
- 好处：灵活，一张表挂多种目标
- 隐患：无外键约束 → 数据库层无法保证引用完整性；**删帖子/商品时图片不会级联删除**，需手动清理孤儿图片；`target_type` 可用枚举（如 Python `Enum` 或数据库 `ENUM`）约束

---

### Q10. 上传第一步校验 `file.content_type`（MIME）。这个字段从哪来？可靠吗？还有什么兜底？

**我的答案：**
file.content_type的字段来自ALLOWED_MIME = {"image/jpeg", "image/png", "image/webp", "image/gif"}，MIME是不是传统的类型，这种奇怪的格式是正确可靠的，除了它还有_compress_image函数中的img = PILImage.open(io.BytesIO(content))真正的去检验这个图片到底是什么

**纠正：** ⚠️ **来源说反了**。
- `ALLOWED_MIME` 是**白名单常量**，不是 `content_type` 的来源
- 真实来源链：浏览器选图 → 按扩展名查系统 MIME 表 → 填进 HTTP 请求头 `Content-Type` → Starlette 解析后放进 `file.content_type`
- **它是客户端"声明"的，可伪造**（curl 传 `type=image/png` 但内容是别的也能骗过第一层）
- 兜底：`PILImage.open()` 读文件**魔数**（真实格式签名）才是真正的体检

---

### Q11. `_compress_image` 里：为什么 GIF 直接返回？为什么 JPEG 要 `convert("RGB")` 而 WebP 可以不动？`img.mode` 的 "RGBA"/"P"/"LA" 是什么？

**我的答案：** 不会

**纠正：**
```python
if img.mode in ("RGBA", "P", "LA"):          # ① 带"麻烦"模式的图
    has_alpha = img.mode in ("RGBA", "LA")   # 是否真带透明
    if content_type == "image/jpeg":
        img = img.convert("RGB")             # ② JPEG 不支持透明 → 丢透明
    elif content_type == "image/webp":
        pass                                 # ③ WebP 支持透明 → 保留
    else:
        img = img.convert("RGB")             # ④ PNG 也转 RGB（存 JPEG 更小）
```
- `img.mode` = 色彩模式：`RGB`（3通道）、`RGBA`（4通道带透明）、`P`（调色板）、`LA`（灰度+透明）
- **为什么必须处理**：JPEG 编码器只接受 RGB，直接保存 RGBA/P/LA 会**报错**
- **GIF 直接返回**：GIF 是动画格式，转 RGB 会丢动画帧
- **WebP 网开一面**：WebP 支持 Alpha 通道，转了反而丢透明

---

### Q12. `img.resize((w*ratio, h*ratio), PILImage.LANCZOS)` —— ratio 怎么算？LANCZOS 是什么？

**我的答案：**
ratio来自上一行的ratio = MAX_DIMENSION / max(w, h)，用设定好的尺寸除以现在图片的尺寸，就得到实际想要让图片变成的大小的百分比，拿去分别成长和高即可；PILImage.LANCZOS可以让图片在压缩大小后，尽量保持不错的画质

**纠正：** ✅ 正确。补充：`ratio = MAX_DIMENSION / max(w, h)` 保证**长边不超过 1600px 且等比缩放不变形**；`LANCZOS` 是重采样滤镜（高质量），比 `NEAREST`/`BILINEAR` 更精细。

---

### Q13. `img.save(out, format=fmt, **opts)` 的 `quality`/`optimize` 控制什么？为什么最后 `return out.getvalue()` 而不是返回 `img`？

**我的答案：** 不会

**纠正：**
```python
out = io.BytesIO()
img.save(out, format=fmt, **opts)   # 把压缩结果"写"进内存文件
return out.getvalue()               # 从内存文件取出 bytes
```
- `format="JPEG"`：用哪个编码器
- `quality=80`：有损压缩强度（0-100，越低越小越糊）
- `optimize=True`：多跑一遍优化，再省 5-10% 体积
- **为什么 `getvalue()`**：`save()` 不返回值，把结果写进 `out` 缓冲区，`getvalue()` 取出缓冲区的 bytes
- **为什么不是 `img.tobytes()`**：`tobytes()` 返回**裸像素数据**（超大、不能直接当文件），`out.getvalue()` 返回**编码后的完整图片文件**——两码事

---

### Q14. 上传接口的 `target_type`/`target_id` 来自 URL 路径，后端没校验 `target_type` 必须是 "post"/"goods"。传 `target_type=evil` 会怎样？应在哪层加校验？

**我的答案：**
有人传 target_type=evil那应该会出错，因为没有这个表，应该在路由层加一个校验，或者直接在orm模型里改列的属性，让它作为枚举，这样更加稳定

**纠正：** ✅ 正确。`target_type=evil` 不会崩溃（只是存进奇怪目录），但绕过了约束。正解：路由层/ schema 校验 + 模型层用枚举双保险。

---

### Q15. `ImageService` 的三个方法都是 `@staticmethod`。为什么？如果一个方法要访问另一个方法会怎样？

**我的答案：**
因为静态函数不用实例化类，而是直接通过类名来调用了，静态函数中调用静态函数，感觉会资源竞争？这个网页不太懂

**纠正：** ⚠️ "资源竞争"是误解。
- `@staticmethod` 的意义：方法不依赖"对象状态"（self），所以无需实例化，直接 `ImageService.xxx()` 调用
- 静态方法内调用同类方法：**必须带类名**（`ImageService.method_b()`），且被调用的也必须是静态方法（不能依赖 self）
- 它真正的限制：**无法访问实例状态**——如果想让 `ImageService` 记住"上次的压缩配置"这类状态（存 self 上），静态方法做不到，得改实例方法

---

## 第三组：私信模块

### Q16. `conversations` 表为什么要求 `user_a_uid < user_b_uid` 排序存储？配合联合唯一约束解决什么问题？

**我的答案：**
这个排序是为了会话不重复，它配合唯一键解决了会话重复的问题，如果没有就会出现比如a和b用户私聊有了会话ab，但如果是b向a发起私信就又会多加一个私信ba的情况，它在功能逻辑上没问题，但我们不需要这样不必要的空间浪费

**纠正：** ✅ 完全正确！"B 向 A 发起又多一条 ba 会话"精确描述了 bug 场景。排序规范化 + `UniqueConstraint(user_a_uid, user_b_uid)` 物理上杜绝重复会话。

---

### Q17. `crud_get_or_create_conversation` 的"幂等"怎么实现？如果两个用户**同时**互相发起私信（并发）会出问题吗？

**我的答案：** 不会

**纠正：**
```python
conv = 查 (a, b)
if conv: return conv          # ① 查到 → 返回
conv = Conversation(...)      # ② 没查到 → 新建
db.add(conv)
await db.commit()
```
- 先查后建（check-then-insert）实现幂等
- **并发问题**：A 和 B 同时发起，两个请求同时"查"，都发现没有 → 都走新建 → **插入两条 (a,b)**
- **数据库兜底**：联合唯一约束让第二条 INSERT 触发 `IntegrityError`（被全局异常处理器捕获），代价是有一个请求失败需重试
- 生产级解法：`INSERT ... ON DUPLICATE KEY` 或捕获 IntegrityError 后重新查询

---

### Q18. `crud_get_conversation` 为什么是"权限闸门"？被哪些方法调用？新接口忘了调用会怎样？

**我的答案：**
crud_get_conversation先检查了是不是有这个会话，当前用户是不是这个会话里的人；它被添加消息和获取消息的函数调用，如果没有它，就有可能出现消息发送到不存在的会话中的情况，前端也会发送连锁错误

**纠正：** ✅ 正确。补充**安全价值**：`current_uid not in (conv.user_a_uid, conv.user_b_uid)` 这行是防"**越权看/发别人的私信**"的关键。任何新接口若不先调它，等于给非参与者开了访问通道——这是集中式权限校验的价值。

---

### Q19. `_load_last_messages` 用 `GROUP BY` + `JOIN` 拿所有会话的最后一条。为什么用 `MAX(id)` 而不是 `MAX(created_time)`？

**我的答案：**
因为时间戳可能重复吧，max（id）更加稳妥，因为id作为主键，完全不可能出现重复的情况

**纠正：** ✅ 正确。id 是自增主键（唯一、单调递增），比时间戳可靠。

---

### Q20. `crud_get_messages` 先 `ORDER BY created_time.desc()` 取分页，再 `messages.reverse()` 转正序。为什么不直接正序取？

**我的答案：** 不会

**纠正：**
```python
result = await db.execute(
    stmt.order_by(Message.created_time.desc()).offset(offset).limit(page_size)
)
messages = list(result.scalars().all())
messages.reverse()   # ← 转正序
```
- **核心矛盾**：聊天页要"最新的在底部"，但分页要从最新的往前翻
- 第 1 页 = 最新 50 条 → `DESC` 取 `offset=0` → 数组"最新在前"
- 但界面要"最老在前"（从上往下读）→ `.reverse()` 转过来
- **不能直接 ASC**：ASC 取到的是"最老 50 条"，翻页就乱
- 所以"倒序取分页 + 反转显示"是标准做法

---

### Q21. `crud_add_message` 里"插入消息"和"更新 `conv.updated_time`"必须同一事务。不更新 `updated_time` 前端会出现什么现象？

**我的答案：** 不会

**纠正：** 会话列表按 `updated_time DESC` 排序。不更新它：
```
豆豆 (最后消息时间: 昨天)   ← 你刚和豆豆聊完，但它还在底下
小红 (最后消息时间: 周一)
```
**你刚发完消息，会话却不置顶**（微信"聊完置顶"就靠这个字段）。所以"插消息 + 更新时间"必须同一事务，否则出现"消息发了但列表顺序没变"的诡异现象。

---

### Q22. 所有接口返回前都用 `_xxx_brief` 把 ORM 对象转 dict。不转直接返回会有什么问题？

**我的答案：** 不会

**纠正：** 看 `User` 模型：
```python
class User(Base):
    email: Mapped[str]
    password: Mapped[str]   # ← 这个！
```
**直接返回 User 对象，FastAPI 会把它所有字段序列化——包括 password（哈希）**。哈希泄露可被暴力破解/离线撞库，是严重安全漏洞。`_user_brief` 是"白名单"——只放行前端需要的字段，其余一律不暴露。

---

### Q23. `get_messages` 响应里带了 `current_uid`。它是干什么的？为什么不能只靠前端 localStorage 判断"哪条是我发的"？

**我的答案：** 不会

**纠正：** 这是修"消息全在左边" bug 的教训。前端 localStorage 的 `user_info` 可能：登录时没存 / 旧格式 / 被清空 → `myUid` 为空 → 所有消息判不成"我发的" → 全在左边。
**解法**：后端从 token（服务器签发、无法伪造）拿 `current_uid` 返回，前端用它判断气泡左右，不再依赖不可靠的 localStorage。

---

## 总评

- 23 题答 15 题，对 12 题，正确率约 80%（答对 12/15）
- 失分集中在：图片压缩细节（Q11/Q13）、并发幂等（Q17）、分页/序列化/置顶的"为什么"（Q20-23）——这些是"只看代码想不通，得懂设计意图"的部分，属于正常盲区
- 消化完本批改，评论 / 图片上传 / 私信 三个模块即全部掌握 ✅

