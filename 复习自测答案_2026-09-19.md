# 复习自测答案 · 校园论坛六大模块

导出时间:2026/9/19 17:07:54
已答 62 / 62 题

## 一、图片模块

### 1.1 [基础] 走链路:从 `POST /api/upload/post/123` 进入,按顺序说出请求经过的每一层、每一步做了什么,直到文件落盘、记录落库、响应返回。要求精确到函数名。

这是我们上传图片的接口，首先注入一个函数来拿到当前用户的信息，这里拿的是uid，然后准备拿图片上传，到达crud层也就是调用upload_image函数，由于它是静态函数，所以不需要先创建类的实例，就直接调用，该函数是粗略检查、请求头校验、控制量读进内存、限制单词上传数量、压缩图片、重命名、最后入库，函数执行完毕后返回成功的响应

### 1.2 [进阶] 上传大小为什么要校验两次(`Content-Length` 预检 + 流式边读边累计)?只保留 Content-Length 检查会有什么漏洞?

校验两次一次是直接检查文件类型是不是图片，领一次是看文件的请求头，因为有的病毒它会伪装成图片，所以直接读文件看是不是图片，流式读取是为了防止文件一次性上传过大把服务器内存用光，只用content_length检查的话就会出现内存耗尽导致服务器崩溃的情况

### 1.3 [进阶] `_compress_image` 为什么要放进 `asyncio.to_thread`?如果直接在 async 函数里同步调用会怎样?对比 `routers/posts.py:141` 同步调用 AI 审核的问题,说说你从这一对代码里看到什么。

这个问题涉及到代码的底层，这个函数声明成异步也不会异步调用，因为函数内部没有可等待的点，所以要用asyncio.to_thread扔到另一个线程去跑，能实现多线程就是靠底层的C扩展，python只是调用而不是执行，所以与python代码是在单线程执行并不矛盾；对比AI审核函数的调用，是同步的，只要也是用同样的操作就能异步调用了

### 1.4 [进阶] GIF 为什么原样返回不压缩?如果强行对 GIF 走 PIL 重编码会发生什么?

因为gif动态图的形式比较特殊，第一步读进内存可以正常进行，但它会成为一个静态图，然后正常转色彩模式、压缩，最后重编码后得到一个色彩可能出问题的静态图，所以比较一下大小就行了，不用做这样的负面功夫

### 1.5 [面试] RGBA/P/LA 模式的图转 JPEG 时为什么必须特殊处理?WebP 和 PNG 的转法为什么不一样?

他们进行色彩模式转换是因为jpeg编码器不支持那几种模式，也就是mode属性，需要转换成RGB来方便用JPEG编码器进行压缩；WebP和PNG有自己对应的编码器，所以他们不需要转码，最后用对应的编码器压缩，或者像PNG这样的只能尽量压缩

### 1.6 [报告关联] 上传接口的 `target_type` 来自 URL 路径且未做白名单校验,代码里直接 `subdir = BASE_UPLOAD_DIR / target_type` 拼子目录。想想 target_type 传 `..` 会发生什么?该怎么修?

路由层确实没做校验，只在模型层写了一个简单注释，原本是想让图片的上传更加灵活，现在是图片应该属于哪个模块全靠接口调用时决定，如果随意传一个字符串的话就会生成一个新文件夹，然后继续存图片，如果是..那就会直接脱离根目录，甚至可以往更上级，我觉得最简单的办法就是直接限制模型层的target_type属性，枚举为posts和goods类型，这样简单又稳妥

### 1.7 [进阶] 删除图片时先 `filepath.unlink()` 再 `db.delete + commit`。如果删文件成功但 commit 失败,数据库和磁盘各是什么状态?反过来先删库再删文件呢?两种顺序各产生什么垃圾、你选哪种?

第一种情况是磁盘里没了但数据库还在，第二种情况是数据库里没了但本地还在。第一种情况是前端读都没法读，但本地还占着磁盘，第二种情况是前端能读但返回出问题。如果要选的话,肯定是副作用更小的那个,我认为数据库里有而磁盘里没有这种不占磁盘只在表里有数据,这种情况相对负面小一些

### 1.8 [面试] images 表为什么没有指向 posts/goods 的真实外键,而用 `target_type + target_id` 两个字符串字段做关联?这种「多态关联」牺牲了什么?

我错了，我以后肯定好好做外键关联，或者至少做枚举来限制，两个字段关联的话灵活度非常高，但安全性也低，代价就像现在这样，我根本没发现会出问题，如果接口的target_type里乱写东西后果会比较严重，轻则文件乱掉重则我也不知道

### 1.9 [进阶] `GET /api/images/{target_type}/{target_id}` 没有任何登录校验,任何人可枚举任意目标的图片列表。评估实际风险有多大,你会怎么改?

这就代表别人不需要登录就能调用接口，我能想到的危害就是开自动化脚本疯狂调用这个接口，然后卡爆我的数据库乃至服务器。至少应该注入访问令牌类的实例来验证登录的用户

## 二、商品与商品评论

### 2.1 [基础] 走链路:发布一个新商品(前端传的是分类**名字**字符串)从路由到落库的完整过程。重点:classify 字符串是怎么变成 classify 列里的整数 id 的?

从接口add_goods出发,前端会根据页面里输入好和选好的内容来发送GoodsCreatePyModel模型,此时的classify也就是str类型,然后到crud层add_goods函数通过分类名称进行查询,拿到结果后的classify_obj就是商品分类模型,于是可以扔掉原本的classify属性,重新赋予id给键classify.;这复杂的操作其实是在为最初模型层属性名混乱的问题擦屁股,后续我会大修模型层

### 2.2 [基础] goods 的主键 gid 为什么用 String(36) 存 UUID,而 posts 用自增 int?两种主键方案各有什么优缺点?

当初的选择并没有考虑这么多,其实两者用哪种方式都无所谓;uuid是唯一的无序的,占空间更大且较长;自增int简单简短但是有序,我并不认为有什么缺点,至少在这个场景下没觉得

### 2.3 [面试] price 为什么用 `DECIMAL(10,2)` 而不是 float?用 float 存金额会出什么事?

DECIMAL是十进制数位直接存,不会出错,而float二进制中0.1是无限循环小数,在无数次的累积运算中会产生越来越大的误差,直接用float存金额会有一点误差

### 2.4 [进阶] 「每人最多 10 个在售」的实现是查出该用户所有在售商品再 `len()`。两个问题:a) 为什么应该用 `func.count()`? b) 两个请求并发上架时这个检查靠得住吗?

我的写法是先拿到全部数据再用len得数量,这样写相当浪费性能,确实应该在写sql语句时就应该直接得到数量才对.        
我觉得靠不住,因为比如现在是9个,但是两个请求在发起时收到的信息都是存在9个,所以会并发上架.

### 2.5 [报告关联] `get_all_goods` 用 `selectinload(Goods.author)` 后把 ORM 直接返回。结合评估报告的实测结论说清楚:响应里为什么会出现作者的 `password` 字段?库里这个字段存的是什么?危害怎么评估?

因为get_all_goods函数中的查询语句里用到了selectinload分别查询了goods模型依赖的另外两个表中的行,而author就对应了User表,里面也就有password字段;这个字段里存的是哈希后的密码;泄露的话如果被离线撞库那么就有可能泄露真实密码

### 2.6 [进阶] model_goods.py 用 `TYPE_CHECKING` 加 else 分支把关联类置为 None,这是在解决什么问题?models/__init__.py 又起什么作用?

这是一个兜底行为,就算TYPE_CHECKING为False也能保证不会导入包,以此避免几个模块互相导入;__init__是枚举所有模型,这样在main模块里导入后相对于把模型都注册进了metadata,便于后续alembic的使用

### 2.7 [基础] status 用 MySQL `Enum('在售','已售出')`。说出 Enum 列至少一个好处和一个坏处,以及坏处触发时要做什么操作。

好处就是简单方便,坏处是加新状态时需要更新数据库,必须新增状态的话就做数据库迁移

### 2.8 [面试] 并发问题:一个在售商品同时被两个人拍下,怎么保证只有一个人改成功?给出用现有技术栈可实现的最简方案。

这个问题我不会,而且本交易平台其实只起了展示作用,并没有实际交易功能,因为牵扯金额事情会变得复杂

### 2.9 [基础] 对比商品评论表和帖子评论表结构,最大的差异是什么?为什么商品评论不需要 parent_id?

两个表最大的差异是信息的流动性吧,因为帖子可以私密,但商品私密就完全没有意义了,以及外键关联不同,依赖也不同;商品评论其实是有父评论的,方便讨论回复

## 三、评论与楼中楼

### 3.1 [基础] 走链路:发一条楼中楼回复(parent_id 非空)从路由到落库的完整过程,包括经验值是怎么加上去的、和评论是不是同一个事务。

前端发来请求,后端调用add_new_comment路由函数,路由内调用crud层的crud_add_new_comment_into_post,然后根据评论者是否是帖子作者来决定通知,在crud层需要先看父评论在不在,因为别人有可能删了,把传的pydantic模型信息给orm模型,然后先落库等加经验,最后一起提交.经验是crud层crud_add_experience函数实现的,和评论不是一个事务,它们是独立的解耦的

### 3.2 [进阶] `crud_add_new_comment_into_post` 里先 `flush` 再 `commit` 而不是直接 commit。flush 和 commit 的区别是什么?这里为什么必须拆开?

flush是先入库,commit是入库后刷盘,虽然数据库都是先记日志再入库,但有没有commit是崩溃恢复的情况分支,如果没有commit那么事务回滚即可,如果commit了那就redo,这里拆开是预防中途崩溃的情况,比如评论后经验没加上崩溃了,那么没有commit就可以回滚而不是重做;还有一个原因是先flush就能拿到自增的id,可以直接用,否则加经验时就拿不到评论者id了

### 3.3 [报告关联] `crud/comments.py:113` 的删除权限判断:`if orm_comment.author_uid != current_user_uid or not user.is_superuser`。手动推演:作者删自己的评论、管理员删别人的评论,各走哪个分支?正确写法是什么?这个 bug 的实际用户感知是什么?

作者删自己是判断出评论者uid为当前用户,管理员删评论是or后面的判断.我这样写确实是很容易错,如果一个人是作者而不是管理员,那么前False后True结果raise异常,导致所有非管理员用户都无法删除评论,我会用更清晰的判断分支来写,如果是作者or管理员则可以删,否则抛异常

### 3.4 [面试] 评论列表为什么只查 `parent_id IS NULL` 的一级评论?楼中楼的 replies 是什么时候、怎么加载的?如果改成一次把所有评论平铺返回,会有什么问题?

因为我们要按照一级评论的量来分页,如果带上子评论那数量就不能确定了.楼中楼的子评论是在查一级评论时用selectinload一起给查了,是在拿到一级评论之后;一次性平铺的话前端无法正常显示了,分页也会出错,我说的是当前前端的情况下

### 3.5 [进阶] 统计一级评论总数的 count 和取列表的查询是两条独立 SQL。为什么 count 不直接复用带 selectinload 的那条 stmt?

首先就是where条件不一样,然后就是查询结果的数据量也不一样,所以不能直接用.但如果非要写一起似乎也行

### 3.6 [基础] Comments.parent_id 自关联外键 `ondelete=CASCADE`:删除一条一级评论时数据库层面会发生什么?这在产品上一定合理吗?

会带着子评论一起删掉,当然合理,因为子评论是附属于父评论存在的

### 3.7 [报告关联] 评论列表接口把带 author 的 ORM 直接返回,密码哈希字段就是这么混进响应的。说清楚机制,并解释 `routers/message.py` 里 `_user_brief()` 为什么没这个问题。

现在似乎没有直接返回用户orm模型的接口了吧.原本会进响应就是把orm给return了,_user_brief没这个问题是这里自定义了字典,而字典里没有密码

### 3.8 [进阶] 评论 content 用 `String(255)`,帖子 content 用 `Text`。选择依据是什么?前端和后端各自该怎么配合这个长度?

因为帖子有时候需要发长文嘛,而评论不需要特别长,且支持子评论.你这么一说我发现我好像并没有检测评论长度,后端可以在pydantic模型层校验,也可以在添加评论时检查评论的content属性的长度,前端我不清楚,我们只复习后端

### 3.9 [面试] 如果给评论加「软删除」(is_deleted),表、查询、权限分别要怎么改?为什么论坛产品几乎都选软删除?

加软删除是为了后续的恢复以防误删误判等情况,应该是表中还在,但是非管理员和作者不能查询,作者可以申请恢复,要加的话可以建一个新表,把删除的评论加进那个表,或者我们直接写后触发器也行

## 四、点赞

### 4.1 [基础] 走链路:`POST /api/likes/toggle` 从进入到返回,按序列出每一次数据库访问(查什么表、为什么查)。

crud层的crud_toggle_like函数先查Posts表是否存在对应的帖子,然后查Like表,看指定用户是否点赞过对应帖子,这里Like是关系表(是帖子对用户的多对多关系),然后看是否已经点赞来决定接下来是点赞还是取消点赞,对应的就是添加数据和删除数据,最后在路由层决定返回信息,如果是新增点赞就加通知,通知是一个专门的字典来做参数

### 4.2 [进阶] toggle 是「先查再插/删」。用户快速双击、或两个标签页同时点赞时会发生什么?最后表里有几条记录?是谁兜住的底?

快速点击和双标签页同时点赞的话会同时发出这个请求,crud层也同时调用,sql语句也会同时执行,但最后表里只有一条记录,因为在orm模型层写了__table_args__,里面用UniqueConstraint来做了一个唯一复合约束,这样才避免了插入相同的数据

### 4.3 [报告关联] `crud_get_like_count` 把该帖全部 like 行查回内存再 `len()`。和 `select(func.count())` 差在哪?数据到什么量级这个写法会疼?

这个确实是个问题,先存内存再读len会占内存,数据量大的话问题就会愈加显现,而select(func.count())是直接用sql语句查到数量

### 4.4 [进阶] 路由层在 toggle 之后又 `select` 了一次 Posts 发通知,而 crud_toggle_like 里已经查过同一个帖子。这算什么问题?「发通知」这件事放在路由层还是 crud 层更合适?说说你的分层观。

crud_toggle_like中查Post是看帖子是否存在,路由里又查了一次是为了确认作者是不是当前用户.这个问题是重复查询了,浪费了性能,发通知应该在crud层,因为它属于数据库的增删改,但实际上应该多一个业务逻辑层,不过我这是小项目就做的简单一些了,这里我应该写一个判断帖子是否存在并返回帖子用户uid的新函数来优化

### 4.5 [基础] 发通知前判断了 `post.author_uid != user_uid`。这条判断防住了什么体验问题?

因为帖子主人自己也可以给自己点赞,这个判断是为了防止自己给自己发通知,这样的通知是不必要的完全多余的

### 4.6 [面试] 帖子详情页要同时显示「点赞总数」和「我是否点过赞」。用现在的表结构最少要几次查询?能不能合成一条 SQL?有没有更进一步的反范式方案?

最少执行两次查询,我觉得完全可以用一条sql就能完成任务,因为我们select的数据是带着所有列的(当然这个以后可以优化),所以能直接拿到userid和数量;反规范化的话就在posts表里增加冗余列like_num,然后用在Like表里用触发器来实现更新

### 4.7 [进阶] likes 表的 `UniqueConstraint(post_id, user_uid)` 一石二鸟:它同时充当了什么?这个联合索引还能顺带加速哪两类查询?

这个复合索引即可以加速where post_id=n and user_uid =n的情况,还能限制数据库中多次出现用户给同一个帖子点赞的记录;能够加速查帖子like量,但单独使用user_uid会失效或加速不明显

## 五、通知

### 5.1 [基础] 走完整链路:用户 A 点赞了 B 的帖子 → B 手机上看到红点 → B 点开通知列表。按顺序说出涉及的表、接口和查询。

后端调用toggle_like路由函数实现点赞,其中查询了posts表确认帖子的存在,然后再查询确认作者
确认作者不是当前用户后发通知,调用了notificationservice.crud_add_notification,是往notifications表里塞数据
B点开通知列表调用接口get_notifications,B看到的红点是因为有未读通知,调用get_unread_count路由函数
点击具体的通知后就调用mark_as_read路由函数,执行crud_mark_as_read函数标记已读,查的都是Notification表

### 5.2 [进阶] `crud_mark_as_read` 用一条 `UPDATE ... WHERE id=? AND recipient_uid=?` 同时完成「改状态+校验归属」。和「先 SELECT 判断 owner 再 UPDATE」相比好在哪?这条 UPDATE 的影响行数在哪些情况下会是 0?

先select再update查了两次表,性能浪费耗时更久,合并成一个sql优化更好;在失败的情况下会是0,比如where里判断没找到

### 5.3 [报告关联] 举报删帖后给双方的通知 `post_id` 都传了 `None`,注释写「避免通知随帖子级联删除」。解释这条链:如果老老实实传 post_id,帖子被删时通知表会发生什么?级联方向是谁定义的?

在notification模型中,有一个post_id外键关联了posts表的id字段,且ondelete写为CASCAD,也就代表关联的那个id被删,当前这个字段对应的数据行也会被删;这个字段允许为null,这样就可以不在乎关联的对象,直接删除这个数据行了.级联方向是数据库定义的

### 5.4 [进阶] notif_type 的模型注释写着 `reply / like / system`,但代码里实际出现了 `report`(点赞、举报都在用)。这暴露了什么维护问题?你打算怎么治理?

虽然这只是一个注释说明,但这也代表我们用这个模型做了它职责范围之外的事情,应该补充说明可以有更多的类型,且为了安全和严谨可以用枚举来限制他能使用的通知类型,本项目的其他模型也有同样的问题

### 5.5 [面试] `/unread_count` 是前端进页必打的接口。写出它现在的查询形态,给出你建议的复合索引(列和顺序),并用最左前缀原则解释为什么是这个顺序。

现在是找到接收者uid且未读的数据,有两个键,复合索引的话用(recipient_uid,is_read),根据最左前缀原则呢就是从左往右匹配列,且不能跳过中间列,这里uid放前面是因为它有顺序且值得查,因为uid会有很多而is_read只有True和False,区分度高的应该更靠左,且等值查询靠左优先于范围查询

### 5.6 [进阶] `crud_add_notifications`(批量)和 `crud_add_notification`(单条)为什么要有两个?举报流程里哪一步依赖批量版?它保证了什么?

单条的是发给普通用户的通知,批量的是发给管理员们的通知,有两个是因为功能的作用和范围不同.举报流程中AI审核可能失效,就需要管理员来审核,于是通知所有管理员;它保证了AI审核失败后尽量第一时间让管理员来处理,且请求一次性发出,性能开销小

### 5.7 [报告关联] 通知列表接口直接返回 ORM 列表(带 sender_uid/recipient_uid 原文和全部列)。对照 message 模块的 `_user_brief()`,写出你认为的通知项理想响应结构,说明每个字段给谁用。

我们这里是把通知的所有列都返回了,有用的没用的都扔给接口再返回前端,确实没必要,但也没危害,因为没有私密信息,优化响应我觉得应该留发送者uid,通知类型,帖子id,内容,是否已读,阅读时间.因为当前的用户已经根据uid收到了后端发来的消息,所以关于自己的信息就不需要了

### 5.8 [基础] `crud_get_user_notifications` 的 count 用了 `select_from(stmt.subquery())`。为什么把带条件的 stmt 包成子查询再 count,而不是把 where 条件重写一遍?

在这里用subquery是因为我们select不能从一个查询语句里继续查,所以要先把它作为一个临时表,然后再接着查,这样就能套娃了.
这里是因为业务需求不一样所以把一个语句分开了,因为通知可以选择性获得已读或未读,所以把stmt这个第一次筛选结果保留,根据需求再做筛选;对比comments获取来说,可以像获取通知这里一样把语句拆分复用,逻辑会更清晰,评论这里后续想添加更多条件就需要继续重写,相对维护更加困难

## 六、实时私信(WebSocket)

### 6.1 [基础] 走链路:浏览器 `new WebSocket("/ws/chat/5?token=...")` 到双方屏幕上都出现消息,按序说出每一步(握手、校验、登记、收发、落库、推送)。

用户点进会话页面时升级连接发生WS握手,校验有多层,先拿当前用户token,再根据token拿到uid,然后计算出当前会话的id并拿到另一位用户的uid,并在connect函数中将连接从HTTP协议改成WS协议,这里用的是accept函数,然后登记进字典并为其添加websocket对象.在while循环中接收信息(接收),并根据会话id存聊天信息(落库),然后发送给两位用户以方便前端渲染(推送和发送),最后网页退出时捕获异常断开链接,这里我做了完善,使用finally来调用disconnect实现清理字典中的键或值

### 6.2 [面试] WebSocket 鉴权为什么用 URL query 传 token,而不是像 HTTP 接口那样放 Authorization 头?这种做法引入了什么新风险?

用websocket.query_params来拿token是因为这个url比较特殊,只能塞请求url中,而不是网站url,所以他不能像其他接口一样用注入access_token_bearer来拿,风险就是会把明文token存日志然后落盘,存token在本地是没有价值的,所以我应该把存token到日志的相关操作都解决掉

### 6.3 [报告关联] ws 路由只调了 `decode_token`。对照 `tools/dependencies.py` 里 AccessTokenBearer 的完整校验链,列出 WS 握手漏掉的每一项检查和对应的攻击场景。

这个问题确实是细节,token是要经过层层验证的,但我刚开始确只是拿token_data做判断导致了很多问嗯题,第一步就是decode后看这个token_data是否存在,没有这个检验的话chat()路由函数会被没有token的机器人调用;第二步是检查是不是访问令牌,没有这一步的话刷新令牌也能进行访问了;第三步是黑名单检测,如果在黑名单中,说明这个token不能用,不能够访问聊天室,如果没有这层检验,就会被进入黑名单的token访问;总的修复方法就是把token的校验全拿过来再写一遍,我选择封装到函数中直接复用

### 6.4 [进阶] 握手时 `crud_get_conversation` 会 `raise HTTPException`。在 WebSocket 路由里抛 HTTPException 会发生什么?和 HTTP 接口里的行为有什么本质不同?

在websocket路由里抛出非WebSocketDisconnect异常,都会导致出现僵尸连接,然后连接会自己关闭;不同就是WS路由中协议升级了没有状态码概念,但是在accept之前抛出异常,仍然可以被收到,因为此时协议没有升级,且如果是accept之前close会被uvicorn硬编码成403,而且WS路由会自己close断开连接,而不是像http请求一样return

### 6.5 [报告关联] `ConnectionManager` 把 `{uid: [websocket]}` 存在进程内存里。a) uvicorn 开 4 个 worker 部署后,A 给 B 发消息会发生什么?b) 为什么说这是部署约束?要支持多进程有哪些思路?

如果有多个websocket实例,那A的每一个websocket实例都会发送这个消息,A给B发消息时,所有的A的聊天框都能看到A给B发的消息;再往后我不会了

### 6.6 [面试] `Depends(get_database)` 用在 websocket 路由上,这个 DB 会话的生命周期是多长?1000 人同时在线聊天会发生什么?你会怎么改?

chat路由里注入了get_database,那么这个会话的生命周期就由聊天页面的时间决定了,时间不定;最大现在数由database_config里的配置决定,为30个连接,所以最多只能容纳30人同时在线聊天,所以在线聊天应该单独写逻辑,比如每一次chat调用都新开一个连接,然后在finally断开

### 6.7 [报告关联] `crud_add_message` 里 `conv.updated_time = datetime.now(timezone.utc)`,而 Conversation 模型的 default 是 `datetime.now()`。这两处写进同一列的时间有什么差别?会话列表按 updated_time 倒序会出什么鬼现象?

一个是时区时间,一个是当前电脑的时间,写进一列的话可能看起来很怪,但没什么实质影响;如果按aupdated_time倒序的话会由于电脑时间的不同发生不同的情况,如果是北京时间就是UTC+8,那么就比更新的时间慢了8消失,就会出现旧会话在新会话前面的情况

### 6.8 [面试] `_load_last_messages` 用 `max(Message.id) group by conversation_id` 再 join 回来取每个会话的最后一条消息。为什么 max(id) 可以近似「最后一条」?这个假设什么时候会破?

因为按会话分组后再找到对应会话里最大id的消息,也就代表它是最新消息,因为id是主键,是自增的,所以消息越新对应的id也会越大;我并不觉得这个写法有问题,它的稳定性要比靠时间来取最新消息更可靠,因为消息可能会同一时间插入多条,但id永远唯一且自增

### 6.9 [进阶] `crud_get_messages` 按时间倒序取一页再 `.reverse()` 转正序。前端拿到的是「最新的 50 条」。要实现聊天窗「上拉加载更早的历史」,第 2 页请求该怎么设计?为什么不改成按时间正序分页?

想要第二页信息只需要调用函数时给page参数为2就行;先按创建时间降序排时为了拿到最新的信息,如果是正序的话,那最新的信息就是在最后一页了,虽然正序拿数据可以直接渲染,但信息数稍微大一些就会没有任何优势了

### 6.10 [报告关联] A-B 会话用 `_normalize_pair` 按 uid 排序后存 user_a &lt; user_b,并配 `UniqueConstraint(user_a_uid, user_b_uid)`。如果没有这两个设计,分别会出现什么 bug?并发首聊时唯一约束扮演什么角色?

这两个设计一个是为了给uid排序防止出现重复的两用户的会话,比如一个a在前一个b在前的重复情况,另一个是复合约束,真正的防止了相同会话的存入;没有排序就会多一个聊天会话,没有约束就会无限制存入会话,有了约束就能保证两个用户的会话只有一个

### 6.11 [基础] `send_to_user` 推给对方的是**裸文本**消息内容,而 HTTP 拉历史用的是 `_message_brief` 结构化字典。如果要把推送也升级成结构化 JSON,收发两端各要改什么?要小心什么一致性问题?

这个我不清楚,我也不太理解如果把推送文本改成结构化字典有什么好处,难道是前端方便渲染吗,这个问题我承认我不会

### 6.12 [进阶] 收消息循环里只捕获了 `WebSocketDisconnect`。如果某次 `crud_add_message` 因数据库抖动抛了别的异常,连接和内存字典会进入什么状态?后续谁会受伤?

如果抛出了别的异常,那么就没法把当前用户从字典中清理掉了,所以需要写一个统一的兜底处理,就是用finally写disconnect逻辑,如果发生了你说的情况,那么字典中uid对应的值会一直存在,甚至会逐渐变多,内存占用变大,且长时间不会被清理,连接关掉,如果用户发了消息经过了死连接,那么会直接抛出异常,然后自己的连接也死掉,于是就不断的产生更多,导致内存泄露直至服务器崩溃.

## 七、全局串联与数据库专题

### 7.1 [面试] 项目里同时存在三种时间写法:`datetime.now()`、`datetime.now(timezone.utc)`、`datetime.fromtimestamp(ts)`。各自出现在哪几类位置?统一成 UTC 的完整方案要动哪几层?存量数据怎么办?

datetime.now()大多是在模型层,因为统一了基类模型Base;timezone.utc的使用是修复token问题时修复的,用到token时间的地方都改了;fromtimestamp是把时间转化为时间戳,方便前端直接核实阻拦;统一的话模型层/CRUD层/工具包里都要改;旧的数据统一数据迁移

### 7.2 [报告关联] 把六个模块里所有「直接返回 ORM 对象」的接口列成一张清单(模块+函数),然后给出你的统一修复方案(response_model 还是白名单函数?),说明选它的理由和改造步骤。

在这个项目中,已经因为我的问题导致用户信息泄露了,刚开始是没想到这个层面,但现在即使明白了风险,手写白名单函数未来很有可能忘掉,虽然白名单函数看起来灵活,但指定返回模型更加简洁方便;response_model是拿到的清洗好的返回的内容,而我们手写的字典如果里面有一些敏感的模型,返回时可能会忘掉.所以用response_model更好,两个东西一起用也没问题

### 7.3 [面试] 按报告建议给六张表补复合索引。每张表写出:索引列及顺序、它服务的查询形态、为什么这个顺序(等值在前/排序在后)。

一个一个说太多了,我直接讲原理,现在项目中很多表都是只有个主键,每个表都补复合索引的话需要根据crud层来写,比如posts表常用crud_get_posts_list,所以根据函数里的需要给(is_top,created_time)建复合索引,根据最左匹配原则,所以is_top在前,不过如果is_top的是否参半,那索引的作用就不大了;再比如就是notification的recipient_uid,is_read,created_time

### 7.4 [进阶] likes 和 conversations 用了 UniqueConstraint「以约束兜底并发」。总结这个模式,再在项目里找出至少一处「没有唯一约束、可以插进重复数据」的场景,并给出建约束的方案。

bookmark模型也是需要唯一约束的,因为一个用户只能收藏一个帖子一次,和like帖子理由一样,方案就是以帖子id和用户uid作为复合唯一约束,本项目中没有除了已做约束的其他orm模型需要这样的约束了

### 7.5 [进阶] 全部列表接口都用 `LIMIT offset, n` 分页。深分页(offset 很大)的问题是什么?私信历史这种「只往前翻」的场景有什么更好的方案?

数据量过大的情况下,数据库依旧会扫过满足条件的所有行,响应时间也会随着数据量的增多而拉长,页漂移的情况也会增多,我们可以按id或者创建时间来分页,用id来判断的话就能不用全查,而是直接根据id算它是第几页,然后我们就直接翻到第几页,或者找到哪些id的内容,这样都要比全表扫描快很多

### 7.6 [面试] `selectinload` 和 `joinedload` 的区别是什么?这个项目为什么几乎全用 selectinload?什么场景 joinedload 反而合适?

两者首先都是解决发过多sql问题的,selectinload是发出第一次查询后,拿到需要筛选的内容后统一用where...in来筛选,主查询结果不受影响,而joinedload是接在主查询后面left join并到主查询中,我用selectinload是因为我只会selectinload,joinedload我觉得适合用在一个结果集返回的一对多情况,比如根据用户去筛选每个用户有哪些在售商品

### 7.7 [进阶] 项目里大量「用字符串当枚举」:images.target_type、notifications.notif_type、goods.status。各自的合法值校验发生在哪一层?漏校验时分别会发生什么坏后果?

图片的校验实在crud层有白名单校验,但,notification的类型没有校验,而是在crud层posts模块直接手写的,商品模型层有枚举限制,也在crud层写;像商品,通知这种没有校验的,那就会出现随便写也不报错的问题,但是前端拿又会出问题,图片在没有改之前,会出现严重问题比如图片名字起..这种就会往上一级走,../..这样还能一直往上;修法就是模型层用枚举然后路由层或crud层校验

### 7.8 [面试] 新需求:给论坛加「帖子标题+内容全文搜索」。现阶段(校园数据量)你会怎么做?到百万级帖子的生产环境演进路线是什么?

这个肯定是模糊匹配搜索,但是需要注意的是%尽量不能放在前,否则可能会失效,所以应该是"用户输入内容%"这样的匹配格式,匹配筛选复合的帖子content和title,我只能想到这里,如果后续数据量大的话我就不清楚更好的方案了
