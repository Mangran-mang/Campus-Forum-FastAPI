#### 5/5 所有接口完成测试可以正常使用了

#### 5/29 加入了使用langchian框架做的测试agent,虽然它没什么用

#### 6/13 

1. posts模型中token不再从前端获取而是后端直接传，新加了置顶判断

2. user模块中的路由函数，全部新增本人与管理员认证

3. crud/posts函数中，引用了or_模块来根据用户显示帖子

   优化了筛选帖子的代码，新增了板块筛选

4. 新增了帖子板块orm模型，并为posts的orm模型新增外键关联至板块id

5. 新增板块创建与更新的pydantic模型

6. 新增了板块(category)的crud函数模块，内有获取、通过id获取、新增、更新、删除模块

   等逻辑，并应用到路由

7. 更新挂载的路由

8. 评论orm模型中，评论新增父评论关联，并添加作者、父评论、依赖映射，同时用户orm模型

   中也关联了自己的所有评论

   pydantic模型中也新增父评论id的写入

   评论的crud模块中，增加父评论校验，通过帖子id返回评论改为只返回一级评论即父评论

9. 新增了点赞、收藏、通知等功能，并完善模型、crud函数、路由函数等内容

​		

#### 6/14

1. 修复了路由函数中注册时检测是否已有账号与通过邮箱查找账号冲突的bug

#### 6/15

1. 移除了头像选择，以及图片上传，遗憾哦
2. 修复了帖子和评论不正常加载头像的问题(删了就是修好了)
3. 修复了用户昵称不正常加载的问题
4. 使个人信息可以正常编辑

#### 6/16

1. 前端做了手机端适配
2. 更换bcrypt版本为4.1
3. 成功从零部署到了服务器

#### 7/6

1. 新增二手交易模块，商品分为多种类型，需要在添加时由用户自行决定类别，且商品会长久存在
2. 添加了商品表、商品分类表，并将商品与用户关联
3. 为商品页添加了评论功能
4. 经过多次更新，管理员的权限越加丰富嘞！

#### 7/14

1. 更新nginx，使其可以正确拿到图片
2. 商品可以展示图片了
3. 修复了alembic数据迁移的问题
4. 管理员可以删除任何信息了
5. 前后端新增了管理员的验证

#### 7/15

1. 根据大家的建议新增了等级系统

#### 7/28

从今天开始，未来所有除了前端的内容更新，都会写出详细的相对路径，更新了什么更新了哪个位置，为什么要更新

#### 7/30

/models/model_bookmark用变量__table_args__添加表级约束

#### 8/3

/schemas/user为email限制类型为EmailStr

/crud/image中file.content_type和PILImage.open()双重防护病毒内容，准确放行图片

/crud/image中实现压缩、识别、放进内存、取出，并在crud函数里二次验证

/crud/image使用pathlib实现灵活上传图片，既可以是商品也可以是帖子

/routers/posts修改了report_post的提交逻辑，将循环遍历管理员提交举报贴，改成了加入列表后统一由add_all提交，仍然高度解耦

/frontend前端页面改为了涂鸦风

#### 8/4

/models/conversation新的私信表

/models/model_user.py做了反向关系映射

/models/model_message.py私信内容表

#### 8/13

/config/redis_config.py为add_jti_to_blocklist增加可选expiry参数，默认仍为1小时，用于按token剩余寿命设置黑名单过期时间

/routers/user修复登出拉黑不完整的漏洞：原实现只拉黑access token的jti，refresh token仍可换新token绕过登出，现改为同时拉黑access与refresh token的jti，并删除数据库中该用户的token记录，让黑名单真正生效

/routers/user登出时黑名单TTL改为按token剩余寿命计算（access约1小时、refresh约2天），使黑名单覆盖token完整生命周期，既不漏拉黑也不浪费缓存空间

#### 8/14

/frontend/src/views/PostDetailView删除帖子详情页"编辑于xx"的显示，该时间因浏览行为被不断刷新，失去编辑时间的含义

/models/model_posts.py移除updated_time字段的onupdate自动刷新机制，避免浏览帖子（浏览量+1触发的update）污染更新时间

/crud/posts.py更新帖子时手动设置updated_time为当前时间，使该字段只在真正编辑时更新，语义回归正确

/ai_agent/review.py新增AI帖子审核模块：独立于闲聊版小柯的审核模型实例（temperature=0、max_tokens=2048），审核prompt只包含帖子标题与正文，输出JSON判定结果，兼容markdown包裹格式，解析失败兜底为未违规避免误删

/config/redis_config.py新增try_report_deduplicate举报去重函数，使用SET NX EX原子操作，24小时内同一用户不能重复举报同一帖子，Redis异常时放行保证功能可用

/routers/posts.py举报接口接入AI审核：同步调用AI审核帖子内容，判定违规则硬删帖子并通知作者与举报人，未违规保留并通知举报人，AI调用失败降级为通知管理员人工审核

/ai_agent/千问小柯.py、/ai_agent/agent_tools.py、/routers/agent.py、/schemas/talk_content.py删除无用的langchain闲聊测试agent及其关联模块，main.py同步移除agent路由挂载与导入

/frontend/src/components/PostCard.vue帖子列表卡片的举报按钮同步升级为AI审核版：增加"AI审核中"loading状态，审核完成后弹窗展示违规/未违规结果，违规则跳回帖子列表，与详情页行为一致

/frontend/src/views/PostDetailView.vue与PostCard.vue统一uid比较方式（转字符串），避免数字/字符串类型不一致导致作者与举报按钮显示误判

/frontend/src/views/PostDetailView举报按钮增加"AI审核中"loading状态，审核完成后弹窗展示判定结果，违规删帖后跳回帖子列表

#### 8/16 - 8/17

将轮询版私信改造为 WebSocket 实时通讯（项目收尾）

/tools/connection.py新增ConnectionManager连接管理器，用内存字典{uid: [websocket对象列表]}记录在线用户连接，提供connect/disconnect/send_to_user三个方法，其中disconnect为同步函数（只操作内存字典），其余为async

/routers/websocket.py新增WebSocket端点/ws/chat/{conv_id}：从URL查询参数取token并手动解码验证，查会话推导对方uid，登记连接后进入while循环收发消息，落库后信号同时推给双方，断开时清理连接

/frontend/src/views/ChatView.vue将setInterval轮询（每2分钟）替换为WebSocket连接：发送消息走ws.send，接收走ws.onmessage收到信号后刷新消息列表，onUnmounted关闭连接

/frontend/vite.config.js新增/ws代理配置（target为ws://127.0.0.1:8000），让前端WebSocket请求转发到后端

/main.py挂载websocket路由，导入方式改为圆括号换行（Python隐式换行语法）

#### 8/26

安全修复：接口返回ORM用户对象导致password（bcrypt哈希）直接泄露进响应体

/schemas/user.py新增UserOutModel输出白名单模型：开启ConfigDict(from_attributes=True)让model_validate可以直接从ORM对象按字段名读取属性，目前只声明uid/email/is_superuser三个字段，password等未声明字段不读取、不拷贝，天然出不去；以后表加新字段也不会意外泄露，想暴露哪个就往白名单里加一行。这玩意学名叫DTO，面试问"接口怎么防止敏感字段泄露"的标准答案

/routers/user.py的current_user、add、get/{email}、update四个接口的data字段全部改为UserOutModel.model_validate(user)后再返回，原来直接塞ORM对象，FastAPI的jsonable_encoder会把User所有属性（包括password哈希）全部序列化出去，登录用户调/all甚至能拿到全站用户的哈希

/routers/user.py的all接口新增UserChecker(True)管理员校验：原来任何登录用户都能拉全站用户列表，现在非管理员返回403；同时删掉冗余的access_token_bearer依赖，因为user_checker内部走的get_user_by_token已经做了完整的token解码+Redis黑名单+查用户，重复挂载等于token验两遍

#### 8/27

P0安全修复收尾：查询接口补认证、登出改POST、登录防用户枚举

/routers/user.py的get/{email}接口新增get_user_by_token认证：原来完全无认证，任何人可以拿邮箱字典枚举探测哪些邮箱注册过，现在必须登录且只能查自己（查别人返回403）

/routers/user.py的logout从GET改为POST：GET的契约是无副作用（浏览器敢预取、敢缓存、历史记录敢重放），而登出会拉黑jti、删refresh token记录，是明确的状态变更，做成GET等于把"终结会话"暴露在任意一次GET重放面前。前端/api/index.js同步给logout加method:'POST'——fetch不指定method时默认GET，后端改POST后前端不改会直接405，登出功能整体失效，这也是"改后端要追前端调用"的一次教训

/crud/user.py新增crud_get_user_by_email_or_none：查不到返回None而不抛UserException，与原方法形成两种明确契约——查不到就该报错的调用方（如get_user_by_token）用抛异常版，需要自己区分"查没查到"的调用方（如登录）用返回None版，docstring写明区别。这是同一个crud契约错配第三次咬人（注册404、get/{email}死代码、登录死代码else全是它：crud抛异常、调用方却按"返回None自己判断"写逻辑），这次把契约显式分开，根治

/routers/user.py登录接口三重防用户枚举：①文案统一——原来密码错误返回401"邮箱或密码错误"，用户不存在却走UserException全局处理器返回404"未查找到对应的用户信息"，攻击者只看响应就知道邮箱注册没注册过；②状态码统一——只统一文案不够，401/400不一致照样泄露，现在两条路径状态码、文案逐字一致；③对齐时序——bcrypt刻意慢(~200ms)，"用户不存在秒回、密码错误跑200ms"的耗时差本身就是枚举信号（时序攻击：不看你说什么，看你答得多快），所以用户不存在时也对预生成的DUMMY_HASH跑一次verify_password，让两条路径工作量相同

/routers/user.py的DUMMY_HASH在模块级用get_password_hash预生成一次常驻内存：必须和真实哈希出自同一个CryptContext（同bcrypt同cost=12），否则假锁规格不同、试锁时长不一致，对齐就失效了

实测验证通过：登录成功242ms、不存在的邮箱214ms、真实邮箱+错误密码243ms——前两者状态码与文案完全一致，耗时差29ms在正常波动范围内，内容侧和时间侧的枚举渠道全部关闭

#### 8/28

P1修复：JWT 有效期时区 bug（access token 实际活 9 小时而非 1 小时）

/tools/security.py的create_access_token中payload['exp']从datetime.now()改为datetime.now(timezone.utc)：原写法是naive本地时间（无时区标记），PyJWT用timegm转换时默认把它当UTC处理，把北京时区的+8小时偏移也算进了时间戳，导致exp虚胖28800秒——签发1小时的token实际活9小时，refresh token活2天8小时。改后exp是正确UTC时间戳，token准时过期。老token全部失效属正常现象（本来就是虚胖的）

/crud/posts.py更新帖子时的updated_time从datetime.now()改为datetime.now(timezone.utc)：该字段存库，若不统一时区基准，与同一张表里已存的UTC时间混用，将来做排序/差值运算会差8小时

/crud/message.py私信会话的conv.updated_time同样从datetime.now()改为datetime.now(timezone.utc)，理由同上

排查结论：/routers/user.py第237、267行的datetime.now()不用改——它们调用了.timestamp()，该方法会自动把naive时间按本地时区解释再转成UTC时间戳，和JWT的exp是同一时间基准（这是"naive直接塞给JWT/存库"和"naive.timestamp()拿去比时间戳"的本质区别）；/crud/notification.py第68、79行的read_time=datetime.now()为遗留的本地时间，建议后续统一为timezone.utc（目前仅判断是否已读，不参与差值运算，暂不炸）

#### 8/29

P1修复：图片上传内存 DoS

/crud/image.py大小校验改为两层：①先查请求头Content-Length，超2MB直接拒（快速通道，multipart part通常不带该头，此层常不触发）；②分块读——每次await file.read(1MB)边读边累计，超2MB立即中断抛400。原来await file.read()一次性把整个文件读进内存再检查，攻击者传2GB请求会把内存吃穿（OOM杀进程），校验形同虚设。改后即使伪造Content-Length，分块累计兜底也只读2MB出头的量进内存

/crud/image.py压缩函数调用改为await asyncio.to_thread(_compress_image, content, file.content_type)：_compress_image是纯CPU同步函数（PIL resize/save），在async路由里直接调用会占死事件循环唯一线程，压缩期间全站所有请求卡住（async解决IO等待、不解决CPU计算）；to_thread把它丢线程池执行，事件循环立刻释放。注意不是给_compress_image加async——async def里没有await点就是纯阻塞函数换马甲，await只能作用于协程/Task/Future，对PIL方法用await直接TypeError

同时前端做了适配

#### 8/31

P2工程完善上半场：日志体系 + CORS 配置化（review 抓出两个启动级 bug）

/tools/log_config.py新增日志配置模块：setup_logging() 用 basicConfig 一次性配置 root logger，挂两个 handler——RotatingFileHandler 落盘 logs/app.log（单文件写满 10MB 轮转成 .1/.2…最多留 5 份，防日志无限增长撑爆服务器磁盘；encoding="utf-8" 否则 Windows 默认按 GBK 打开文件，中文日志乱码）+ 无参 StreamHandler 走控制台，开发时 uvicorn 窗口直接可见。log_dir 用 __file__ 两层 dirname 锚定项目根而不写相对路径"logs"：服务器上 systemd/nohup 启动时工作目录不一定是项目根，相对路径会把日志散落到随机位置；makedirs(exist_ok=True) 保证重复启动不报错。level=INFO 是全局门槛，所有模块 logger 默认沿层级向上传播到 root，所以一处配置管全项目，各模块只管 getLogger(__name__) 打日志

/main.py建 app 之前调用一次 setup_logging()（全进程只生效一次，必须放在最前）；lifespan 里的 print("项目已完全启动") 换成 logger.info，logger = logging.getLogger(__name__)

/tools/middleware.py请求日志正式启用：原来写好了 message 却注释掉的 print，现在是 logging.getLogger("app.http").info(message)，格式"IP 方法 路径 状态码"。"app.http" 是自定义标签不是必须，好处是 [%(name)s] 显示 app.http，想单独看请求日志直接 grep；业务模块统一用 __name__（天然是模块导入路径，出处一目了然）。教训：之前四个模块写的 from fastapi import logger——它本质就是 getLogger("fastapi")，所有日志名字全显示 fastapi，等于白配了格式里的 %(name)s，出处信息全丢，已全部改回 __name__

/tools/middleware.py CORS 从写死 allow_origins=["*"] 改为 [o.strip() for o in Config.CORS_ORIGINS.split(",")]，从配置读多域名（生产 zcfe.online，开发 localhost:5173，逗号分隔）。踩坑：第一版写成 [Config.CORS_ORIGINS.split(",")]——split 本来就返回列表，外面再套一层 [] 变成列表套列表，starlette 判断 origin in allow_origins 永远 False，所有跨域请求被浏览器拦。本地不联调根本发现不了，上线前端全挂，review 实测抓出来的

/config/config.py Settings 新增 CORS_ORIGINS: str 字段。连带踩坑：第一版 .env 里把我写在 config.py 的 Python 示例整行（CORS_ORIGINS: str = "..."）贴了进去，python-dotenv 只认 KEY=value，解析失败整行跳过，字段缺失 → Settings() 实例化抛 ValidationError → import main 启动即崩。.env 里改回纯键值对后实测 STARTUP_OK。两个职责别混：.env 管键值对，config.py 管字段声明与默认值。另注意该字段没给默认值——服务器 .env 下次部署必须补 CORS_ORIGINS=https://zcfe.online，否则线上启动即崩

/tools/exceptions.py全局异常处理器接入日志，分级记录：db/sqlalchemy/other 这类真异常用 logger.exception 记完整堆栈（线上排障全靠它）；http_exception_handler 区分——4xx 是业务流程（登录失败、404），只 logger.warning 记状态码和 detail，5xx 才 exception。原因：HTTPException 大多出现在正常分支，爬虫扫一圈 API 全是 ERROR+堆栈，10MB×5 的轮转很快被刷穿

/config/redis_config.py三处 print 换模块 logger，消息恢复各自语义：写入黑名单失败/查询黑名单失败/举报去重失败（第一版图省事全写成"检查缓存失败"，排查时会误导自己）；is_jti_in_blocklist docstring 补设计取舍：Redis 不可用时降级放行（返回 False），代价是故障窗口内已拉黑 token 可继续用；不做 fail-closed（异常即拒绝）是因为那等于 Redis 一挂全站登出——可用性优先的取舍，面试要能讲清为什么这么选

/requirements.txt锁定 passlib==1.7.4 + bcrypt==4.0.1（与实际安装一致，消除 passlib 读取 bcrypt 版本的兼容告警）。遗留问题：fastapi==0.110.0、sqlalchemy==2.0.0、PyJWT==2.8.0 钉的是当年的下限值，环境里实际装的是 0.136.0/2.0.49/2.12.1，重装环境会降级装老版本，要么改回 >= 要么 pip freeze 生成 lock 文件，别停在中间态

/.gitignore增加 alembic.ini、logs/

实测验证：.venv 里 import main 通过（.env 修复后 Settings 正常加载，logs/ 自动创建），启动链路无报错

#### 9/1

响应格式统一改造（前后端各两阶段，前端适配待做）

背景：项目里返回响应三种写法混用——return 字典、return JSONResponse、raise 异常，且不少接口"HTTP 200 + body 里写 400/404"，前端只能读请求体判断成败。本次把后端响应彻底收敛为一种结构，前端最后统一适配。

统一后的格式（成功与失败共用同一套骨架）：

    {"code": <HTTP状态码>, "message": "<中文描述>", "data": <业务数据或null>}

code 直接用 HTTP 状态码，不另立业务码表，前端 res.status 与 body.code 完全一致。

阶段一：异常响应统一

/tools/exceptions.py重写：新增 APIException 业务异常基类，用类属性声明 status_code / default_message；UserException(404) / PostException(404) / CommentsException(404) / GoodsException(404) / AIException(502) 全部改为继承基类（AIException 此前没注册 handler 会落 500 兜底，现按 502 语义返回）。新增 error_response() 统一构造错误响应体，message 只放能给用户看的中文，SQL/堆栈/原始异常文本只进日志不回传（避免泄露表结构）。新增 _status_phrase() 判断 detail 是否为框架默认英文短语（"Not Found"），是则换成中文兜底文案

/main.py的register_exception_handler重写：原来每个业务异常单独注册一个 handler（post_not_found_error/user_not_found_error/comments_not_found_error + GoodsException 走工厂），状态码和文案硬编码在 handler 里，几种异常就得写几个 handler；现在只注册一个 APIException 即可覆盖全部子类——Starlette 查找 handler 时遍历 type(exc).__mro__，子类沿继承链上溯自动命中父类的处理器。顺带删掉失效的 @app.exception_handler(500)：它用了全局变量 app 而非函数参数 fapp，且注册数字 500 只对 HTTPException 生效，对真正的未捕获异常完全无效，从来没生效过

踩坑：HTTPException 必须注册 starlette.exceptions.HTTPException 而不能只注册 fastapi.HTTPException——fastapi 那个是 starlette 的子类，路由没匹配上时 Starlette 抛的是父类，只注册子类会让框架级 404/405 沿 MRO 找不到 handler、仍然返回旧格式 {"detail": "Not Found"}；注册父类后子类沿 MRO 上溯照样命中，两者都覆盖

/tools/exceptions.py新增422参数校验handler：默认 422 返回 FastAPI 原始 errors 数组，与其它错误结构不一致，现统一格式且把字段级明细（field/message）放进 data，是全项目唯一 data 非 null 的异常场景，供前端表单回显

阶段二：成功响应统一

/tools/exceptions.py新增 success_response(data=None, message="ok", code=200) 与 error_response 成对，routers 全部收编，禁止再手写 return 字典 / JSONResponse

/routers/下 13 个文件全部改造（bookmark/category/like/comments/goods/goods_classify/goods_comment/image/message/notification/posts/user）：手写 {"code":200,...} 换成 success_response，补齐缺失的 data 字段，清理"删除评论状态True"这类把 bool 拼进 message 的怪文案（bool 挪进 data）

假成功响应全部改 raise（核心修复）：/routers/posts.py 举报接口 3 处——帖子不存在 raise PostException()、举报自己/重复举报 raise APIException()（基类默认 400，不再传状态码）；/routers/user.py 的 get/update/delete 三处"用户不存在" raise UserException()。改后 HTTP 状态码与 body.code 一致，前端可以直接按状态码判断

结构性变化（前端第三阶段要适配的两处破坏点）：①登录接口 access_token/refresh_token/user 从顶层挪进 data（/routers/user.py 的 login/refresh_token/logout 三个 JSONResponse 改 success_response），LoginView.vue 读 res.access_token 会拿不到；②商品评论列表 total 从平级挪进 data.total（/routers/goods_comment.py），GoodsDetailView.vue 读 res.total 会变 undefined。其余 res.code===200 / res.message / res.data.xxx 前端本就兼容，res.detail 消失但前端都有 || 兜底

验证：后台起 uvicorn + curl 实测 10+ 场景全过（200 成功 / 业务 404 / 框架 404 中文 / 405 / 举报自己 400 / 举报不存在 404 / 注册 / 登录 / current_user），测试数据已清理

踩坑（验证方法论）：TestClient（httpx ASGI transport）+ aiomysql 全局连接池存在事件循环复用问题——每次请求开新事件循环，连接池里的旧连接绑在之前的 loop 上，同一 TestClient 连续请求会随机 500，误导排查。结论：本项目验证响应格式用真实 uvicorn + curl（单事件循环，无此问题），不用 TestClient 打真实接口

顺带发现的两个遗留问题：①板块表 categories 是空的，发帖传任何 category_id 都会外键失败；②db_exception_handler 把 FOREIGN KEY 错误统一翻译成"用户不存在"，实际可能是分类/其他关联不存在，文案误报待修

#### 9/3

token 明文存库修复（P2）：refresh token 改存 sha256 哈希

概念确认：先论证并否决了"转码 + 接口返回转码值 + 前端解码"方案——可逆编码 + 还原逻辑在前端可翻，等于明文；而哈希是单向不可逆的，且正确设计下前端根本不需要解码（接口永远返回原始 token，只是数据库里那份变成 64 位哈希，验证靠"客户端交上来的 token 再哈希一次去比对"）。防的是"拿到数据库的人"（SQL 注入/备份泄露/接口误吐字段——之前 /all 把 password 哈希吐给全站就是同款 bug），不是翻前端源码的人。

改动：
- /tools/security.py 新增 hash_token()：sha256 单向哈希，无盐。为什么不用 bcrypt：bcrypt 的慢哈希是给低熵密码防暴力破解的，JWT 是 256 位高熵随机串，快哈希即可（GitHub 存个人访问令牌同理）；为什么不需要盐：盐防相同低熵密码跨用户撞库，token 本身每把都唯一
- /models/model_token.py：token 表 refresh_token 列语义改为存哈希（注释同步），新增 jti 列 String(64)
- /crud/token.py：add/update 落库前 hash_token(token)，并写入 token_data["jti"]
- /tools/dependencies.py：TokenBearer 校验通过后把原始 token 塞进返回字典（raw_token 键，仅内存传递）——库里只有哈希，刷新接口必须拿原始串再哈希才能比对
- /routers/user.py：/refresh_token 增加哈希比对——顺手修掉原来"只查行存在、不比对 token 本身"的隐患（那等于谁拿到任何一把没过期的旧 refresh 都能刷）；/logout 改读 jti 列 + expire_at 拉黑（库里已是哈希解不出 jti，所以 jti 必须单独明文存，jti 不是凭证，泄露无害）
- 迁移 a1b2c3d4e5f6：token 表加 jti 列；存量明文 refresh_token 原位 decode → sha256 + 回填 jti，解不出来的脏行删除（过期/伪造本来就刷不了）；本机库已执行，用户会话不中断（客户端仍持原始 token，刷新时再哈希命中）

冒烟测试（uvicorn + 真实 HTTP，Redis 在线）全绿：库中 refresh_token=64 位 hex 且=sha256(登录返回的token)、jti 36 位；当前 token 刷新 200；被顶掉的旧 token 401；登出后库行删除、Redis jti 拉黑生效。

可讲的设计点：
- 哈希输入里混 email/uid/exp 这些"已知信息"无害——sha256 安全性靠输入熵 + 单向性，不靠输入保密；真正该警惕的是"只哈希一部分"，取到低熵可猜字段（如 email）就能离线穷举撞库
- sha256 的"256"是输出长度（bit）= 64 个十六进制字符，与输入长度无关，token 再长落库都是 64 位
- 库里"哈希 + jti 明文"组合不矛盾：哈希那列是凭证指纹（不可用），jti 列只是会话身份证（光有它刷不了，仅供登出拉黑）

#### 9/4

refresh token 轮换（P2）实现

概念澄清（和用户讨论后的结论）：轮换 ≠ 登录时覆盖 refresh。登录覆盖是"重新认证"（前提是用户又输了一次密码）；轮换管的是两次登录之间、免密刷新路径上 refresh 是否每次换新。原状：/refresh_token 只发新 access，refresh 从登录起 2 天固定不变、可无限次换 access——泄露窗口=剩余 2 天且受害者无感。轮换价值：每把 refresh 只能用一次（泄露只能利用一次），旧 token 被二次使用会立刻暴露（受害者下次刷新 401）；配合每把重给 2 天 = 自带滑动续期。

改动：
- /routers/user.py /refresh_token：哈希比对通过后新增两步——签发新 refresh（refresh=True，2 天新 jti）→ crud_update_token 覆盖库行 → 返回 {access_token, refresh_token}（原来只返回 access_token）。旧 refresh 不需要额外拉黑：单行模型下覆盖即作废，下次哈希比对必然失败
- /frontend/src/api/index.js：
  - doRefresh 成功后 saveTokens(access, refresh) 同时保存新 refresh——轮换是前后端联动，前端不替换本地旧 refresh 会死循环 401
  - tryRefresh 改单飞（共享同一个 promise）：access 过期瞬间页面常有多请求并发 403，各自发起刷新会拿同一把旧 refresh 互踩——轮换前无害，轮换后第一个成功其余全 401 误登出；单飞让所有请求等同一个刷新结果

决策：不做并发宽容窗口。代价：双标签页同秒刷新时后到者 401 被踢重登，概率低可接受。

冒烟测试 11 步全绿：登录→库=hash(R1)；refresh(R1)→200 返回 R2 且库=hash(R2)；旧 R1 再用→401（一次性作废）；refresh(R2)→200 返回 R3 连续轮换；再次登录后旧 R3 再用→401（登录覆盖仍生效）；登出删行。

git：commit bd44c97「安全加固：refresh token 哈希存储 + 轮换机制」+ 6f49608「9/4 公告」，待用户自行 push。

遗留盘点（9/4 全库逐文件核对，见 .workbuddy/memory/2026-09-04.md）：
- 真实邮箱验证：零代码，修复记录方案未落地
- 自动化测试：无 tests/ 目录
- 🔴 帖子评论 total bug：/comments/getcomments 返回 data=列表、total 没返回（crud 算了没用上），PostDetailView 读 res.total 恒 undefined → 帖子详情"评论 (0)"、超一页无下一页；9/1 响应格式统一那轮只适配了商品评论，漏了帖子评论
- 🟡 categories 板块表仍空（9/1 记录至今）：发帖页板块下拉无选项
- 🟡 exceptions.py FOREIGN KEY 分支仍统一翻译"用户不存在"（9/1 记录待修）
- 上线 checklist：服务器 .env 补 CORS_ORIGINS=https://zcfe.online（无默认值，缺了启动即崩）；服务器跑迁移 a1b2c3d4e5f6 + 重启；前端重新 npm run build 部署（轮换依赖前端存新 refresh，旧 dist 会 401 死循环）；确认部署依赖文件（只有 requirements.lock 无 requirements.txt）

#### 9/12

后端模块复习（图片/goods/ORM/模型基建/评论/点赞/通知/私信模型）中发现的 5 个问题批量修复：1 个让删评功能整体失效的真 Bug、1 个路径穿越漏洞、3 项性能与安全加固

/crud/like.py的crud_get_like_count改用select(func.count()).where(Like.post_id == post_id)，取result.scalar()：原实现是select(Like)把该帖所有点赞行全查出来再在 Python 里len()计数，5000 个赞就要传 5000 行过网络、造 5000 个 ORM 对象、并占着 session 的 identity map 到请求结束（identity map 是所有经手的对象按主键登记的仓库，用于保证同一行只有一份对象副本、避免重复查询；只读数据点一下就永远不用，白占），只为得到"5"这个数字。改到数据库端聚合后只返回一行一列，Python 侧不再有任何对象化开销。注意 COUNT 在 InnoDB 里仍是 O(N)（要把匹配的索引条目逐个数过去），省的是常数与传输——with MVCC 每个事务看到的行数可能不同，无法像 MyISAM 那样缓存一个总行数。WHERE 里出现了 Like 的列，FROM 能自动推断，所以不需要 select_from（对比 /crud/notification.py 里 select(func.count()).select_from(stmt.subquery()) 那种写法：SELECT 列表只有函数、WHERE 又为空时才必须手动补 FROM）

/crud/comments.py的crud_delete_comment_by_comment_id权限判断写反（真 Bug）：原条件 `orm_comment.author_uid != current_user_uid or not user.is_superuser` 是"写拦截条件时对放行条件取反却忘了翻转运算符"——放行条件是 `是作者 or 是管理员`，其否定按德摩根定律应为 `not(是作者) and not(是管理员)`（或变与），写成 or 之后语义变成"不是（作者且管理员）就拒绝"，于是普通作者删自己的评论 403、管理员删别人的评论也 403，四种身份组合里只有"既是作者又是管理员"能通过，删评功能对所有人失效。现改为正向判断：`if orm_comment.author_uid == current_user_uid or user.is_superuser:` 则执行删除、提交、返回 rowcount>0，否则 403。正着写就不会碰取反，从根上避免这类笔误（同项目 /crud/goods.py 里同款权限判断用的是 and，本是正确对照，可作参照）

/crud/image.py的upload_image新增target_type白名单：`if target_type != "post" and target_type != "goods": raise HTTPException(status_code=400, detail="目标文件夹禁止上传图片")`，位置紧放在 `subdir = BASE_UPLOAD_DIR / target_type` 之前。原实现对 target_type 没有任何校验就拼进路径：传 ../config 这类值会被 BASE_UPLOAD_DIR / "../config" 解析成 uploads 目录之外的路径，随后的 mkdir(parents=True) 照样创建、图片照样写到项目根下（经典路径穿越 CWE-22），同时还能在 uploads 下凭空制造任意垃圾目录。取值定为 post/goods 是查证后的结论：models/model_image.py 的列注释写的是 `关联类型(post/goods)`（单数 post），前端 CreateGoodsView.vue 调的是 imageApi.upload('goods', ...)，全局搜不到 'posts' 这种写法。状态码用 400 而非 403——这是参数非法，不是身份够不够的权限问题，403 会把排查方向带偏。校验点必须在 write_bytes 之前才拦得住，放在这里时压缩已经跑完（传非法 type 会先解码+缩放+重编码再被拒），属"先干活后拒绝"的小浪费，想省就再往函数头挪

/schemas/goods.py新增GoodsAuthorOut、GoodsClassifyOut、GoodsOutModel三个输出模型，/routers/goods.py新增模块级辅助函数_goods_out()并在add_goods、get_goods、get_goods_by_gid、update_goods四个接口应用（delete_goods无返回数据不用改）：与 8/26 用户模块是同一个问题——接口直接把 ORM 对象塞进 data，FastAPI 的 jsonable_encoder 会把对象身上所有已加载属性原样序列化出去。实测确认了机制：jsonable_encoder 有个默认参数 sqlalchemy_safe=True，作用是"排除所有以 _sa 开头的字段"，即只挡 _sa_instance_state 这一个 SQLAlchemy 内部状态对象，业务字段一个都不筛（fastapi/encoders.py:189-195 定义、274-277 过滤）。而 crud 里 selectinload(Goods.author) / db.refresh(..., ["author"]) 把完整 User 加载了进来，于是商品列表里每个作者都带着 password 哈希和 email 出现在响应体里。goods 模块不是没方案，是没跟上 user 模块 8/26 就立好的白名单约定，本次照抄。两处设计取舍：①作者信息单独建 GoodsAuthorOut 而不复用 UserOutModel——后者含 email，服务的是"自己看自己"，给别人看的场景必须收得更窄（uid/nickname/level 足够），邮箱、is_superuser 这类字段泄露给外部等于把攻击面直接送出去；②price 在输出模型里声明为 float 而不是 Decimal——FastAPI 的 Decimal 编码器（encoders.py 的 decimal_encoder）输出的是数字，而 Pydantic v2 默认把 Decimal 序列化成字符串（实测 Decimal("12.50") → "12.50"），若直接声明 Decimal，前端拿到的就是 string，会被 toFixed 之类的数字运算打崩。嵌套结构靠字段名与 ORM 关系属性对齐（classify_rel / author），model_validate 一次递归完成三张表的转换；每个嵌套模型都要自己写 from_attributes=True，这个配置不被父模型继承。位置选择：转换放路由层而非 crud 层——crud 的 get_goods 同时被 update_goods / delete_goods 当内部工具用，那些逻辑依赖 ORM 对象的 setattr 与 refresh 能力，改返回模型会把内部逻辑打崩，保持"crud 返回领域对象、路由负责对外形态"的分层。顺带收益：模型校验会读关系属性，一旦某处漏了 selectinload，异步下会直接抛 MissingGreenlet 当场炸，而不是静默返回残缺数据

/routers/posts.py的report_post补上await：改为 `await asyncio.to_thread(review_post_content, post.title, post.content)`。review_post_content 是同步函数（内部 review_model.invoke 是阻塞 HTTP 调用，docstring 写着通常 2-5 秒返回），在 async 端点上直接调用会把事件循环唯一线程占死，期间全站所有请求一起卡住（async 只解决 IO 等待、不解决阻塞调用）；而 asyncio.to_thread 本身是 async def，漏掉 await 只会得到一个 coroutine 对象——下一行 review_result.get() 立刻抛 AttributeError: 'coroutine' object has no attribute 'get'，这个异常又正好落在本函数原有的 try 里，被降级分支吃掉，最终表现为"每次举报都通知管理员 AI 审核暂不可用"，接口照旧返回 200 加一段合情合理的提示语，AI 审核静默失效且不报错。这是"业务降级逻辑把代码写错和依赖挂掉混为一谈"的典型陷阱：降级分支本意是防 AI 服务不可用，结果连自己的语法/语义错误一起兜住了。参数在主线程求值（post.title / post.content 先算成字符串再交给线程），线程里拿到的是纯字符串、不会碰 ORM 对象或懒加载，用法本身是安全的

以上 5 处已逐个读代码确认落地。建议实测三条路径：①普通账号删自己的评论应成功、管理员删别人的评论应成功（改前两条都是 403）；②POST /api/upload/board/123 应返回 400，且 uploads 目录下不能新增文件夹；③商品列表响应里全局搜不到 password / email，且 price 是数字而非字符串。举报接口要确认返回的是 AI 审核结果，而不是"AI 审核暂时不可用，已转交管理员处理"——出现后者说明 await 又漏了或 AI 侧真的挂了，看日志里有没有 AttributeError 可区分

遗留（本次未修，见 .workbuddy/memory 的待修清单）：/crud/notification.py 的 count 子查询可简化、posts/category/comments/bookmark/goods_comment/notification 等路由仍裸返回 ORM 对象（同类泄露风险待逐个确认）、/crud/image.py 的 PNG 透明转 RGB 黑底与 sort_order 并发竞态、/schemas/goods.py 输入模型的 price 仍是 float

#### 9/13 - 9/14

纯复习期，无代码改动（期间只在本日志补写了 9/12 条目）。继续走后端模块复习线，发现的问题全部并入待修清单，9/15 起才动手处理。这两天最值得记的两条发现：

- **私信模块的 relationship 定义了却没用**：models/model_message.py 里写了 conversation / sender 两条关系，但 crud/message.py 全程手写批量查询再把结果挂成临时属性（m._sender、conv._other_user、conv._last_message），_load_last_messages 里的子查询其实就是在手写 selectinload。结果是"两头各写一半"——背了 relationship 的概念负担，又没省下手写组装代码，还因为属性名是 _sender 对不上 Pydantic 的 from_attributes，只能继续手写 _message_brief() 拼字典。用起来还是删掉，需要二选一。
- **/routers/message.py 的 _user_brief() 泄露 email（与 goods 同类的隐私问题复发）**：它返回 username/email/is_superuser，被用在 conv.other_user、msg.sender，以及 **/api/messages/users/search 的搜索结果**上。而 crud_search_users 支持按 email 模糊匹配（User.email.like(kw)）——搜一个 "@qq.com" 就能批量捞到别人的邮箱和管理员身份，等于把 8/27 刚修好的防邮箱枚举从后门重新打开了。修法与 goods 同款：给对外可见的作者信息单独建白名单模型（uid/nickname/level），不要复用带 email 的 brief。

#### 9/15

WebSocket 认证管线补齐（安全）+ 前端断线自动重连（可用性）

/routers/websocket.py新增模块级 async 函数authenticate_ws_token()，把 WS 路由的认证从"只验签"补成完整的三步，路由内改为 `token_data = await authenticate_ws_token(token)`。原实现的根因在 9/15 之前的改动里已经埋下：因为浏览器的 WebSocket API 不允许自定义请求头（`new WebSocket(url, protocols)` 第二参只能是子协议名、没有 headers 选项），token 只能塞进 URL 查询串，于是无法像其它接口那样注入 AccessTokenBearer 依赖，而当时只保留了 decode_token 一步。对比 /tools/dependencies.py 的 AccessTokenBearer 完整做四件事：①decode_token（验签+过期）②token_valid（非空）③verify_token_data（**拒绝 refresh token 冒充**）④is_jti_in_blocklist（**查 JTI 黑名单**）——WS 侧②③④全丢了。其中④最要命：本项目的登出功能正是靠 add_jti_to_blocklist 把 jti 写进 Redis 实现的（/routers/user.py），HTTP 侧每个接口都查黑名单、WS 侧不查，形成"登出对 HTTP 生效、对 WebSocket 不生效"，用户点了登出只要浏览器还开着聊天页，那条连接依然活着还能重连。修法是把三步抽成一个共用函数供两边调用，避免再出现"一边查一边不查"。校验点仍在 accept() 之前（失败即 close(1008) 且不登记），所以不留名册残留。

顺带核实的两点：①decode_token 对 None / 空串 / 乱码 / 数字**全部返回 None**（PyJWT 抛的 DecodeError 是 PyJWTError 的子类，被 except 接住），所以删掉原 `if not token` 的提前判断是安全的、行为等价；②但 decode_token 用的是 logger.exception，会把正常业务情况（token 无效/过期）按异常打完整堆栈，与项目自身"4xx 走 warning、5xx 走 exception"的约定冲突，且缺 token 现在也会走到这里 → 每次不带 token 的连接都刷一段堆栈（扫描器随便连一下就刷屏），建议改 logger.warning。另 routers/websocket.py 里那行被注释掉的 disconnect 理由写错了——不是"忘了加 await"，真理由是会与 finally 重复调用、多端在线时抛 ValueError（list.remove 不幂等）。

/frontend/src/views/ChatView.vue断线自动重连。原实现只有 onmessage，没有 onclose / onerror，连接断了前端毫无感知：用户点发送只弹一句"连接尚未就绪"，既不知道发生了什么，也没有任何自愈动作。改动：新增连接状态（connecting / open / reconnecting / failed）并在顶栏显示；onclose 里做指数退避重连（1s→2s→4s→8s→10s 封顶）；加重复建连守卫（readyState 为 CONNECTING/OPEN 直接 return，避免同一账号重复登记）；token 每次重连重新读 localStorage 而不缓存进闭包（HTTP 层刷新过 token 后能自动吃到新的）；onerror 留空——浏览器 error 之后必定还会触发 close，重连只写在 onclose 里，否则两个回调各发起一次重连、退避计数被打乱；onUnmounted 里**先置 closedByUs = true 再 close()**（顺序不能反，否则 close 触发的 onclose 会再排一次重连 → 离开页面后幽灵重连）；onopen 里补一次 loadMessages() 回填断线期间对方发的消息；发送失败保留草稿、断线时主动踢一次重连并给明确文案。顺带把 .chat-hint 在移动端的 display:none 改成只隐藏"实时消息"这类装饰文案——断线提示在移动端恰恰是最需要看到的。已 npm run build 验证通过。

#### 9/16

WebSocket 路由改短会话（连接池架构修复）+ send_to_user 逐条容错 + 前端品牌更名与两处修正

/routers/websocket.py改短会话，解决"聊天页长期占用数据库连接、把全站连接池吃光"的架构问题。根因链条：①fastapi/routing.py 的 websocket_session 里，AsyncExitStack 包在 `await func(session)` 之外——你的整个 chat()（含 while True）都跑在这个 with 块里，**出了块才关 exit stack、才会执行依赖的 teardown**，所以 get_database 的 `async with AsyncSessionLocal()` 退出、session.close()、连接归还，全部发生在 chat() 返回的那一刻，也就是聊天页面关闭时；②而 SQLAlchemy 的 session 是惰性借连接的——第一次 execute() 就 autobegin 一个事务并借走连接，直到 commit/rollback/close 才归还（用 engine.pool.status() 实测：刚创建 session 时 Checked out = 0，跑一次 SELECT 后 = 1，闲置 0.5 秒仍是 1，commit 后才回 0）；③路由里 crud_get_conversation 的 SELECT **在进 while 循环之前**就把连接借走了，且没有 commit，于是连接从"进入路由"一直被扣到"页面关闭"。本项目的池是 pool_size=10 + max_overflow=20 = 30 且 **HTTP 与 WebSocket 共用同一个 async_engine**，所以不是"聊天只能 30 人"这么温和——N 个开着的聊天页会让池里只剩 30−N 给整个 HTTP API，N 接近 30 时全站接口借不到连接，等 pool_timeout（默认 30 秒）后抛 TimeoutError → 500，即"聊天页一多，连看帖子、登录一起挂"。改法：去掉 `db: AsyncSession = Depends(get_database)`，改为两处短会话——查会话信息用 `async with AsyncSessionLocal() as db:` 并在块内取出 other_uid（块外对象已 detached，访问属性会抛 DetachedInstanceError），循环内每条消息再开一个短会话落库。效果是把连接的持有时间从"页面打开时长"（分钟/小时级）压缩到"单次 SQL 耗时"（毫秒级），池上限的含义从"同时在线的聊天页数"变成"并发执行的 SQL 数"，两个数量级不同。

/tools/connection.py的send_to_user加逐条容错。原来是无保护地 `for ws in connections: await ws.send_text(text)`，一个坏连接就能中断整轮推送。实测（uvicorn 0.44.0）往已断开的连接 send_text 会抛 **RuntimeError: Unexpected ASGI message 'websocket.send', after sending 'websocket.close' or response already completed**，不是静默失败——于是连锁反应是：for 循环中断 → 排在后面的活连接收不到消息；异常继续上抛穿透 while 循环（那里只 except WebSocketDisconnect）→ 发送者自己的连接也被 uvicorn 关掉（实测连接状态变 CLOSED）→ 而发送者同样是异常退出、也会变成僵尸 → **一个僵尸杀死一个无辜连接并制造第二个僵尸，会自我扩散**。修法是两层互补：`if ws.client_state != WebSocketState.CONNECTED: continue` 负责挡"已经知道断了的"（Starlette 的 receive() 在读到 websocket.disconnect 时会把 client_state 置为 DISCONNECTED，而僵尸之所以是僵尸正是因为它经历过 receive_text() 抛 WebSocketDisconnect，所以这个状态必然已生效），单条 try/except 负责挡"刚在发送这一刻断的"（竞态窗口）。另外确认了一点：**这里不需要顺带清理字典**——边遍历边删会漏元素（实测 5 个元素的列表只访问到 3 个，被删元素后面的那个会被跳过），如果漏掉的恰好是活连接就又丢消息；清理的职责归 disconnect，而每条连接自己的 chat() 协程会在 finally 里调它，所以僵尸最终会被它自己清掉，send_to_user 只负责跳过。

/frontend/品牌名替换与构建产物清理：郑财论坛 → 郑财校园墙，只改显示文案，未动变量名/路由/localStorage key/接口字段，涉及 index.html 的 title、AppHeader.vue、AppFooter.vue、AnnouncementView.vue（15 处版本名）共 4 个源文件 18 处。顺带清理了 dist 的历史构建残留：vite.config.js 设了 `build.emptyOutDir: false`（注释说由部署脚本统一清理），导致 dist/assets 累积了 8 个历史版本的 js 和 9 个 css，旧 js 里还带着改名前的字样——已删除 17 个非本次产物。**这个项目的 dist 不会自动清，改完前端务必确认 assets 目录里没有旧 hash 文件混着，否则全量覆盖上传会把旧产物一起带到服务器。**

/frontend/src/views/ChatView.vue修正一处死分支。9/15 写的重连逻辑里用 `e.code === 1008` 判断鉴权失败并借 HTTP 层刷新重试——实测证明这行**永远不会执行**：后端在 accept 之前调用 close(1008) 时，uvicorn 会把它变成 **HTTP 403 响应**（源码里是硬编码的 FORBIDDEN，close code 被丢弃），浏览器看到的是 `InvalidStatus: server rejected WebSocket connection: HTTP 403`，onclose 的 code 是 **1006** 而不是 1008——握手阶段的拒绝拿不到 close code。改为按"连续握手失败次数"兜底：加 everOpened 标记（区分"握手失败"与"连上后又断"）与 handshakeFailures 计数，连续失败达 5 次就停手，新增 failed 状态（"连接失败，请刷新页面重试"），send() 在该状态下不再空踢重连。已 vite build 验证。

同日顺带查清的两件事（属协议层认知，记下备查）：①**WS 路由抛异常与 HTTP 路由抛异常的关键差异**——starlette/middleware/errors.py 的 ServerErrorMiddleware 开头就是 `if scope["type"] != "http": 直接透传`，所以注册在 Exception（500）键上的兜底 handler 在 WebSocket 路由上**根本不会被执行**（HTTP 侧则会，实测 HTTP 抛异常客户端能拿到 500 + 完整 JSON 信封，WS 侧 handler 未被调用、客户端只拿到 1006）。要让 WS 也能被结构化处理，得用 WebSocketException + websocket_exception_handler（handler 必须返回 None 并自己关连接，返回 Response 会在 websocket scope 上发 http.response.start 而触发 uvicorn 的 RuntimeError），实测该路径客户端能拿到 code=1011 + reason。②状态码只在握手阶段存在：accept 之前 close(1008) → HTTP 403、raise HTTPException(404) → HTTP 404（走 ExceptionMiddleware 的 status_handlers，不经过 ServerErrorMiddleware）、raise 任意异常 → HTTP 500；accept 之后一旦 101 升级完成就没有"状态"概念了，只剩 close code（0–4999）+ 最多 123 字节 reason。

改动过程中因实现细节出过两个致命错误并已修复，记下来避免重犯：①`token_data = authenticate_ws_token(token)` **漏 await** ——函数是 async def，不加 await 拿到的是 coroutine 对象，而协程对象是真值，导致 `if not token_data` 这个守卫被静默绕过，下一行按字典取下标抛 `TypeError: 'coroutine' object is not subscriptable`，异常发生在 accept 之前、每条连接一建立就崩（幸好 fail-closed，不会建立未认证连接）。**这是本项目第二次犯同类错误**（第一次是 9/12 report_post 里的 asyncio.to_thread），规律很明确：凡调用 async def 必须 await，漏了不报错、只是拿到一个"还没执行的东西"。②`async with AsyncSession()` **用错了类**——AsyncSession 是会话基类、需要 bind，不带 bind 实例化不报错但一执行 SQL 就抛 `UnboundExecutionError: This session is not bound to a single Engine or Connection`，应该用已绑定 async_engine 的工厂 AsyncSessionLocal()。两处修完后验证：py_compile 通过、`import main` 装配成功、WS 路由正常注册、AsyncSessionLocal 绑定的驱动是 mysql。

遗留（截至 9/16，详见 .workbuddy/memory/2026-09-15.md 与 2026-09-16.md）：
- 🔴 WS 路由的短会话改动**尚未做端到端验证**（本地无 MySQL/Redis，只做到装配级）。服务器上可用 `SHOW STATUS LIKE 'Threads_connected'` 作硬指标：打开 3~5 个聊天页后该数字应基本不变，若每开一页 +1 且不回落则说明还有长事务挂着
- 🟡 WS 路由未注册 WebSocketException 处理器（代码已在 9/16 实测验证过）
- 🟡 posts/category/comments/bookmark/goods_comment/notification 等路由仍裸返回 ORM 对象
- 🟡 /routers/message.py 的 _user_brief() 仍泄露 email（含 /api/messages/users/search 的枚举通道）
- 🟡 私信模块 relationship 用起来还是删掉，需二选一
- 🟡 decode_token 的 logger.exception 建议改 logger.warning
- 🟡 多 worker 部署盲区：连接注册表与连接池都是模块级单例、每进程一份。若 uvicorn 开 --workers 4，则 WS 跨进程推送会丢、且理论最大 MySQL 连接数变成 4 × 30 = 120（接近 max_connections 默认 151）；须确认 systemd 里没配 --workers
- ⏸️ 运维侧（同学 9/16 决定暂缓）：/etc/logrotate.d/ 已确认含 nginx 且 /var/log/nginx 每天轮转保留 14 天（**磁盘写满风险排除**）；但 token 会以明文进入 Nginx access log（token 在 URL 里），且 uvicorn 自己的 WebSocket accept 日志也带查询串（uvicorn/protocols/utils.py 的 get_path_with_query_string 会把 query_string 拼进日志，走 uvicorn.error → stderr → journald）。风险有限（access token 仅 1 小时有效 + 日志 14 天轮转删除），修法是一行配置（Nginx 把 log_format 里的 "$request" 换成 "$request_method $uri $server_protocol"，$uri 不含查询串；或该 location 直接 access_log off）

#### 9/17 - 9/18

私信模块的 relationship 收口：把"手写批量查询 + 挂临时属性"换成 selectinload

models/model_message.py 里 conversation / sender 两条 relationship 早就定义了，但 crud/message.py 全程没用它们，而是手写 `select(User).where(User.uid.in_(sender_uids))` 再 `m._sender = senders.get(...)` 挂成临时属性——**两头各写一半**：背了 relationship 的概念负担，又没省下手写组装代码。更糟的是挂的属性名带下划线（`_sender`），而 Pydantic 的 from_attributes 只按字段名找，所以 `_message_brief()` / `_conversation_brief()` / `_attach_other_user` 只能继续手写拼字典，泄露 email 的问题（见 9/13-9/14 那条）就是从这个缝里长出来的。

改造分三步走完：①`crud_get_or_create_conversation` 的 `refresh(conv)` 之后，把 `_attach_other_user` 里那 8 行字典推导删掉，改成 `selectinload(Conversation.user_a)` / `user_b` 直接加载；②`crud_get_messages` 和第 125-134 行那两处字典推导一并删掉，主查询挂 `selectinload(Message.sender)`；③`crud_add_message` 的 `refresh(orm_msg)` 改成 `refresh(orm_msg, ["sender"])`。

三处都改完后有个概念要记住，这里实测澄清过：**`refresh` 是"逐条"，`selectinload` 是"批量"**。`refresh(msg, ["sender"])` 只对一条消息补加载，列表场景下用它就是 N 次查询；`selectinload` 是 `WHERE user_uid IN (...)` 一次性捞回来。所以不是"哪个更好"，是哪种场景配哪个——单条操作用 refresh，列表用 selectinload。另外 `refresh` 的本质是"丢弃 + 重读"：**不带 attribute_names 的 `refresh(obj)` 会把对象上【所有】属性（包括关系）全部过期**，之后读关系就变成异步懒加载 → `MissingGreenlet`。这个坑 9/19 和 9/21 又各踩了一次。

#### 9/19

全站挂 response_model（51 个路由，0 遗漏）+ 修掉挂了三周的帖子评论 total bug + 统一时间基准

**为什么要挂 response_model**：9/12 修 goods 泄露时是"想起来哪个就手写一个 `_goods_out()` 转一次"，但 posts / comments / bookmark / goods_comment / notification 那批路由仍然 `return success_response(data=orm对象)`。实测确认了一件反直觉的事：**`success_response` 只负责"包装"，完全不负责"过滤"**——同一个函数，挂了 response_model 的接口 password/email 被挡住，没挂的原样出去。也就是说"信封"不提供任何保护。而项目里没有任何机制保证你不忘，所以必然有漏网的。

改法先建类型层的信封 `schemas/common.py::Envelope[T]`（`Generic[T]`，`data: T | None`）——它和 `success_response` 是同一件事的两个面：一个运行时造结构，一个类型层描述结构。然后写各模块的 Out 模型，最后逐个路由挂上去。**关键规则：`response_model` 校验的是【整个返回值（信封）】，不是 data 里的东西** —— 单个用 `Envelope[PostOut]`，列表用 `Envelope[list[PostOut]]`，无数据用 `Envelope[None]`。`return` 那行一个字都没改。

**这次改造顺带暴露并修掉了几个被掩盖的问题**，这是它最大的价值：

1. **`jsonable_encoder` 不触发懒加载，`response_model` 会。** 旧的 `success_response(data=orm_obj)` 走 `jsonable_encoder`，它用 `vars(obj)`（即 `__dict__`），**未加载的关系根本不在 `__dict__` 里** → 表现为"字段静默缺失"，不报错；`response_model` 会真的去 `getattr` 每个字段 → 读到未加载的关系 → **异步懒加载 → MissingGreenlet → 500**。所以挂模型会把"漏预加载"全部逼出来：`crud_add_new_post` 只加载了 author、漏了 category；`crud_update_post` 的 `refresh(orm_post)` 会把关系也过期；`crud_add_comment` / `crud_add_comment`（商品）的 replies 没加载。

2. **`Posts.updated_time` 的 `onupdate` 加错了，已撤掉。** onupdate 的语义是"这一行只要发生 UPDATE 就触发"，不是"只有编辑才触发"。而 `get_post_by_id` 每次浏览都会写一次 `view_count`（`post.view_count = (post.view_count or 0) + 1` + commit），于是 `updated_time` 被"浏览"不断刷新，变成"最后被浏览时间"。**实测：只改 view_count，updated_time 也会跟着变。** 撤掉 onupdate，编辑时间由 `crud_update_post` 显式赋值维护。**注意 `Conversation.updated_time` / `Goods.update_time` 的 onupdate 是合理的**（那两张表没有"浏览一下改一列"的业务），不要一起改。

3. **`/report` 曾误挂 `Envelope[PostOut]`**：它的 data 是 AI 审核结果（`{violated, type, reason}`），而且判定违规时帖子已经被删了。误挂会抛 9 个 validation error。已新建 `ReviewResultOut`。

4. **`/api/messages/users/search` 的 email 泄露修掉了**：原来走 `_user_brief()`（返回 email），而 `crud_search_users` 支持按 email 模糊匹配 → 搜一个 "@qq.com" 就能批量捞别人的邮箱和管理员身份。改走共享的 `UserBriefOut`，搜索能力保留、只是不回显邮箱。

5. **输出模型的组织方式**：抽了一个共享的 `schemas/user.py::UserBriefOut`（uid/nickname/username/level/is_superuser），所有"别人看我"的场景共用。判据是"**不同场景该暴露的字段是否真有分叉**"：User 有（email 只给本人）所以拆 `UserOutModel`（自己看自己）/ `UserBriefOut`（别人看我）；Category、GoodsClassify 没有分叉就共用，不重复定义。另一个判据是"**同一个东西的不同深度用继承**"（`_CommentBase` → `CommentReplyOut` / `CommentOut`）。

**顺带修的帖子评论 total bug**（9/4 记录，挂了整整两周）：`/getcomments` 只返回评论列表、`total` 被丢掉了，而前端读的是 `res.total`（平级）→ 恒为 undefined → 0，表现为标题永远显示"评论 (0)"、分页条也永远不出现。后端改成 `data={"total": total, "comments": comments_list}`，前端改读 `res.data.comments` / `res.data.total`。**商品评论的 `list` 字段一并统一成 `comments`**（`list` 会遮蔽 Python 内建名，而且两个几乎一样的"评论列表"接口用不同字段名迟早出事）。

**时间基准统一**：全项目盘点后发现 21 个 DATETIME 列里只有 `posts.updated_time` 用了 `datetime.now(timezone.utc)`（还写成了传值不传函数的形式，等于永远返回模块导入时刻），其余 20 列都是本地时间，而 `crud` 里有 4 处手写 UTC。服务器时区实测是 `Asia/Shanghai (UTC+8)`，所以两边差 8 小时。已统一回服务器本地时间。**但 `tools/security.py` 的 JWT `exp` 必须保留 `datetime.now(timezone.utc)`** —— `exp` 是绝对时间点（Unix 时间戳），而 **PyJWT 会把 naive datetime 当成 UTC 解析**，实测用本地时间会让 token 晚 8 小时过期（1 小时的 access token 变成 9 小时），是安全削弱。

#### 9/21

三个安全漏洞修复 + 删用户改成级联删除

**① 提权漏洞（两条路径，都是实测跑通后才确认的）**

评估报告只说了"UserCreateModel 不该有 is_superuser/is_active"，但实际情况比"模型字段不干净"严重得多：

- **路径 1：不登录就能创建管理员账号。** `/api/user/add` 没有任何鉴权依赖，而 `crud_add_new_user` 是 `User(**user.model_dump())` —— 请求体带 `is_superuser: true` 就直接落库。实测 `POST /api/user/add {"email":..., "password":..., "is_superuser": true}` → 200，数据库 `is_superuser = 1`。域名是公开的，等于一条 curl 就能拿到管理员。
- **路径 2：任何登录用户能给自己提权。** `/update` 的权限判断是 `if current_email != user_data.email:` —— **改自己就跳过管理员检查**，而 `UserUpdateModel` 里有 `is_superuser`，`crud_update_user` 用 `model_dump(exclude_unset=True)` + `setattr` 直接落库。实测四步全通：注册 → 登录 → `POST /api/user/update {"email":"自己","is_superuser":true}` → 响应 True → 用同一个 token 调 `GET /api/user/all`（仅管理员接口）返回 200。

根因是 **Mass Assignment（批量赋值）**：`is_superuser` / `is_active` 不是"用户能自选的属性"，而是**系统对用户的授权结果**，把它放进"用户输入模型"就等于让被授权者自己决定自己有没有权限。

修法：两个输入模型删掉这两个字段（Pydantic 默认 `extra='ignore'`，客户端传了也进不来）+ `crud_add_new_user` 里显式兜底 `is_superuser = False`（防止以后有人加回来）。**改完复测两条路径都关了**（响应 False、数据库 0）。

**关于"那管理员怎么来"——这是个引导问题（bootstrap problem）**：要调管理员接口你得先是管理员，循环依赖，所以**第一个管理员必然产生在系统之外**（SQL / 配置 / 部署脚本），这是所有权限系统的共同结构，不是本项目缺陷。同学选择"以后手动在服务器敲 SQL"（`UPDATE user SET is_superuser = 1 WHERE email='...'`），对小项目合理——没有接口就少一个攻击面。

**② `is_top` 漏洞（同类问题，另一个模型上复发）**

`PostsCreateModel` / `PostsUpdateModel` 里有 `is_top`，于是任何注册用户发帖时传 `is_top: true` 就能置顶。同学一开始认为"is_top 是显示自己帖子列表的置顶，不是首页，而且前端没实现"，**实测证明这个理解与代码不符**：`crud_get_posts_list` 的 `ORDER BY is_top DESC, created_time DESC` 写在过滤条件【之前】，对所有列表查询都生效——`author_uid` 只是后面加的可选 WHERE，排序不受它影响。实测：先发一条置顶的、再发一条不置顶的，调不带任何筛选的 `/get_posts`，**先发的（更早）排到了第 1 位，把别人的帖子全挤下去了**。前端确实 0 处引用 is_top，但**前端只是按返回顺序渲染**，顺序变了用户直接能看到。

修法（方案 A）：两个输入模型删掉 `is_top`，新增**管理员专用接口** `POST /api/posts/admin/set_top/{post_id}`（body `{"is_top": true/false}`，挂 `UserChecker(True)`），crud 新增 `crud_set_post_top()`。实测三场景全过：普通用户发帖带 is_top → 无效；普通用户调管理员接口 → 403；管理员调 → 置顶/取消都成功。

**③ 删用户改成级联删除（改了两层，中间失败过一次）**

原本"有帖子的用户删不掉"：`posts.author_uid` 等 5 条外键只写了 `onupdate` 没写 `ondelete`，MySQL 默认 NO ACTION → 有数据就拒绝删除 → `crud_delete_user` 直接 `db.delete(user)` 没兜异常 → 500。实测发现是"**用过的用户基本都删不掉**"（发过一个帖/评论过一次/上传过一张图/发布过一个商品，任意一种都挡住）。

**第一层**：5 条外键（`posts.author_uid` / `comments.author_uid` / `goods.author_uid` / `goods_comment.author_uid` / `images.author_uid`）加 `ondelete="CASCADE"`，配套新迁移 `d1e2f3a4b5c6`（沿用数据库里已有的约束名 `posts_ibfk_1` 等，因为当初建表没写 name、是 MySQL 自动生成的；MySQL 不支持改已有外键的 ON DELETE，只能删了重建）。

**但改完数据库、迁移跑通，删用户仍然失败** —— 报错变成了 `IntegrityError (1048, "Column 'author_uid' cannot be null")`，SQL 是 `UPDATE posts SET author_uid=%s WHERE posts.id = %s`。

**根因：SQLAlchemy ORM 删父对象时，默认行为不是"交给数据库级联"，而是自己动手** —— 它把子集合加载进来，逐个把外键**置为 NULL**（nullify）。这些列都是 NOT NULL，直接炸。**数据库那条 CASCADE 根本轮不到生效，ORM 先插了一手。**

**第二层（关键）**：给 `User` 上 9 个指向子表的关系全部加 `passive_deletes=True`（`posts` / `token` / `comments` / `goods` / `goods_comments` / `images` / `conversations_as_a` / `conversations_as_b` / `messages`），意思是"别管子集合，交给数据库处理"，这时 CASCADE 才真正跑起来。（`notifications` 不需要：它没有 User 上的 relationship，只有两个裸 FK 列，不经过 ORM。）

**→ 教训：数据库外键和 ORM 关系必须【成对改】，少一边都不行。**

改完实测：删一个"有帖+有图、且别人在他帖子下有评论"的用户 → 200「删除成功」，user / posts / images 全部级联归零；迁移可逆（`downgrade -1` 回到 NO ACTION，`upgrade head` 回到 CASCADE）。

**⚠️ 方案 A 的既定代价（实测看到了）**：删 A 之前该帖下有 1 条评论（**别人写的**），删 A 之后变 0 —— **"删一个用户"会连带抹掉别人在他帖子下的发言**。这是硬删除的本质，不是 bug。哪天想改成"内容保留、身份消失"，走软删除即可（`is_active=False` + 匿名化），前提是先让 `is_active` 真正生效。

**④ `is_active` 是个死字段**

全项目只有"定义"和"出现在输入模型里"，**没有任何一处读它**——不在 `tools/dependencies.py` 的鉴权流程里，也不在登录里。所以 `UPDATE user SET is_active = 0` **拦不住任何人**（照样能登录、发帖），"封号"功能目前根本不存在。要做的话加在 `get_user_by_token`（它会查库；`TokenBearer` 只验 token、不查库，加在那里看不到用户状态变化）：

```python
if not user.is_active:
    raise HTTPException(status_code=403, detail="账号已被停用")
```

#### 本次部署清单（⚠️ 后端 + 前端 + 迁移，三样都要）

**1. 后端**
```bash
# 拉代码后重启服务
systemctl restart <你的服务名>
```
**特别注意**：本次改动面很广，`schemas/` 下新增了 `common.py`（信封）和 `image.py`（图片输出模型），`crud/`、`models/`、`routers/` 三个目录都有文件被改，**别漏传**。

**2. 数据库迁移（必须做，否则删用户的行为和本地不一致）**
```bash
alembic upgrade head
```
本次新增迁移：`d1e2f3a4b5c6_用户外键改为级联删除.py`（新 head 是 `d1e2f3a4b5c6`，父版本 `a1b2c3d4e5f6`）。

**⚠️ 已知障碍**：`alembic.ini` 在 `.gitignore` 里（因为里面是明文 root 密码），**所以纯 `git pull` 拉的服务器上没有这个文件，alembic 跑不起来**。需要单独传一份上去，或者按下面的改进建议改造成从 `config.Config` 读 URL。

想验证迁移是否生效：
```sql
SELECT TABLE_NAME, DELETE_RULE FROM information_schema.REFERENTIAL_CONSTRAINTS
WHERE CONSTRAINT_SCHEMA='mangran' AND REFERENCED_TABLE_NAME='user';
-- 13 条应该全是 CASCADE
```

**3. 前端（必须和后端【一起】上）**

本次改了两个 `.vue`：`PostDetailView.vue`（评论列表读 `res.data.comments` / `res.data.total`）、`GoodsDetailView.vue`（`res.data.list` → `res.data.comments`）。

- **只上前端**：后端还返回 `data` 是数组，前端读 `res.data.comments` → undefined → **评论列表空**
- **只上后端**：前端还读 `res.data`（现在是对象）→ 也空
- **所以要么一起上，要么先上后端、立刻上前端**

**⚠️ 这次构建后 `dist/assets/` 里混了 3 组历史产物**，`index.html` 只引用最新那一组：

| 文件 | 状态 |
|---|---|
| `index-DK0OTCym.js` + `index-CucyfBiQ.css` | ✅ **本次真正要用的**（index.html 引用的就是这组） |
| `index-pGJx5Gvk.js` + `index-CKw_Stzu.css` | ⚠️ 旧（9/19 那次构建的） |
| `index-BrMKV1zQ.js` + `index-DRMsHXmI.css` | ⚠️ 旧 |

**这个项目的 `vite.config.js` 设了 `build.emptyOutDir: false`（注释说由部署脚本统一清理），dist 不会自动清** —— 旧 js 里可能还带着改掉之前的文案/逻辑。**全量覆盖上传会把 6 个文件一起带到服务器。** 上传前确认部署脚本有没有清（照 9/16 那次的做法：只保留 `index.html` 实际引用的那一组）。

**4. 部署后要做的两件事**

- **给自己加管理员**（本地实测管理员数是 0，服务器上如果也是 0，所有管理功能都用不了）：
  ```sql
  UPDATE user SET is_superuser = 1 WHERE email = '你的邮箱';
  ```
- **抽查两个接口**：①帖子详情页评论数显示是否正常（不再是 0）②`/api/posts/admin/set_top/{id}` 能否用管理员 token 调通

#### 本次遗留（下次继续）

- 🔴 **`is_active` 没有检查**（见上，封号功能不存在）
- 🟡 **模型索引那批尚未落地**：已定的 5 项（`images` 的 `UniqueConstraint` 改成 `Index(target_type, target_id, sort_order)`；`messages` 的改成 `Index(conversation_id, created_time)`；`notifications` 的改成 `Index(recipient_uid, is_read, created_time)`；`posts` 新增 `Index(is_top, created_time)`；`crud/message.py` 排序加 `, Message.id.desc()` 做 tie-breaker）。**原计划的三个 UniqueConstraint 会破坏功能**（`images` 那条会让同一个目标传不了第二张图，`messages` 那条因为 `created_time` 只到秒会让同秒两条消息冲突），必须改成纯索引。
- 🟡 **孤儿图片文件**：`ondelete="CASCADE"` 只删数据库行，磁盘上 `uploads/` 里的文件还留着
- 🟡 `alembic.ini` 被 gitignore 导致 clone 下来跑不了 alembic → 建议让 `migrations/env.py` 从 `config.Config.DATABASE_URL` 取 URL，ini 里留空，这样 ini 就能进版本库
- 🟡 `crud/notification.py`：count 的 `select_from(stmt.subquery())` 可简化为直接 where、`scalar_one_or_none` 建议改 `scalar_one`
- 🟡 `crud/image.py` 质量项：PNG 透明转 RGB 黑底、落盘 `write_bytes` 同步阻塞、`target_id` 不校验目标存在、`sort_order` 并发竞态
- 🟡 `decode_token` 用 `logger.exception` 打正常业务情况（token 无效/过期）的完整堆栈，与项目"4xx 走 warning"约定冲突
- 🟡 多 worker 部署盲区：连接注册表与连接池都是模块级单例每进程一份，须确认 systemd 里没配 `--workers`

#### 9/25

私信会话查询去 N+1 + 前端错误提示统一化 + token 唯一约束（多窗口同时登录修复）+ 异常文案分流

**① 私信会话列表消除 N+1**（`crud/message.py`）

`crud_get_user_conversations` 原来在 for 循环里逐个调 `_attach_other_user`，每次都 `await db.execute(select(User).where(...))` —— 一页 20 个会话就是「1 次计数 + 1 次主查询 + 20 次用户查询」= 22 次数据库往返，会话越多越慢。改法：主查询挂 `joinedload(Conversation.user_a)` + `joinedload(Conversation.user_b)`（两条关系都是 to-one，一条 SQL 带两个 LEFT OUTER JOIN 就拿全，不会像一对多那样产生笛卡尔积膨胀），`_attach_other_user` 从"查库"降级为"纯内存挑选"。SQL 从 `3+N` 降到固定 3 条，与页大小无关。

⚠️ 踩的坑：这个函数被**三条路径**调用，只给其中一条补预加载是错的。`crud_get_or_create_conversation` 的"查已有会话"分支补了 `joinedload` 没问题，但"新建会话"分支里 `conv.user_a` 从来没被赋值过、`db.refresh(conv)` 又只刷新本表列不带关系，改完后会在那里触发**异步懒加载 → MissingGreenlet → 500**。这个坑最难发现——**只有"第一次和某人私信"才会走到**，已有会话的场景永远正常。修法是用函数开头已经查出来的 `other_user` 直接挂：`other_uid` 本来就是"相对当前用户的对方"，语义天然正确，不需要 `is_user_a` 判断。

**② 前端错误提示统一化**（新增 `frontend/src/utils/errorMessage.js`，改 11 个 .vue）

起因是注册填 6 位密码只提示"校验错误"。查下来是**三层叠加**：①`RegisterView.vue` 前端写 `minlength="6"`，后端 `UserCreateModel.password = Field(..., min_length=8)` —— 前端放行、后端拒绝；②`tools/exceptions.py` 的 422 响应 `message` 是笼统的固定文案"请求参数校验失败"，**真正的字段级明细在 `data` 里**（`[{field, message}]`）；③全项目 14 处错误提示都写 `res.detail || res.message || 'xx失败'`，而 `res.detail` 在新信封下**根本不存在**（后端早改名成 message 了），是死代码，于是永远落到那句笼统文案上。

改法：新增 `pickErrorMessage(res, fallback)` 统一入口，优先级 `data` 字段明细 > `message` > fallback，内含 Pydantic 英文校验消息 → 中文的映射（`String should have at least 8 characters` → `密码至少 8 位`）。**映射表是先跑探针拿到真实的 Pydantic 报错字符串再写的，不是凭记忆编正则**——实测覆盖 `string_too_short` / `value_error`(邮箱) / `missing` / `string_type` / `float_parsing` 五种。匹配一律用宽松正则，匹配不到返回原文（宁可显示英文，也不要吞成"未知错误"）。

顺带两个决定：①**前端不再用 HTML 的 `minlength` 传达规则** —— 浏览器原生校验发生在 `submit` 事件**之前**，它不满足就轮不到 `@submit.prevent` 绑的 handler，自己写的中文提示永远没机会显示；规则改写在 JS 里，另配常驻 `<p class="field-hint">`（placeholder 一输入就没了）。②`RegisterView` 加 `validate()` 本地预校验，提示精确到"密码至少 8 位（当前 6 位）"。

**③ 修掉 GoodsDetailView / PostDetailView 共 20 处 `catch {}` 空块**

任何一步失败页面都毫无反应，用户只会觉得"点了没生效"然后反复点 —— 静默失败比报错更难排查（用户没法告诉你"控制台报了个 500"）。改为写 `pageError`（页面顶部 `<p class="error-msg">`，复用 main.css 全局类），纯装饰性的加载（如帖子配图）失败只 `console.warn`。

**顺带修掉两个独立 bug**：`handleDelete` 原来拿到响应就 `router.push` —— 删失败（403 无权 / 404 已不存在）也会跳走，用户以为删掉了、回头看列表东西还在，现在必须先确认 `res.code === 200` 再动；`toggleStatus` 同理，否则会出现界面显示"已售出"、数据库里还是"在售"，刷新一下就穿帮。

**④ `token.user_uid` 加唯一约束**（`models/model_token.py` + 迁移 `6f75ba267425`）

`routers/user.py` 的登录写 token 是 check-then-act：先查该 uid 有没有 token 行 → 没有就 `add`。**两个并发请求会读到同一个未提交快照**（InnoDB 默认 REPEATABLE READ，普通 SELECT 是快照读、不加锁）→ 都判定为 None → 都 add → **出现两行**。之后 `crud_get_token_by_user_uid` 的 `scalar_one_or_none()` 抛 `MultipleResultsFound` → 被兜成 500 → **该用户从此登录/刷新/登出全部 500**，永久故障直到手工删行。`crud_add_token` 里那道"再查一次"的检查也挡不住 —— 两次检查落在同一个窗口内。

修法：给 `token.user_uid` 加唯一约束，让**数据库**保证"一个 uid 一行"。应用层的 check-then-act 永远有窗口，正确的做法是把不变式交给唯一索引。（注意：唯一约束只保证"不出错"，要让并发登录**都成功**而不是第二个报 400，还得把 check-then-act 换成 MySQL 的 `INSERT ... ON DUPLICATE KEY UPDATE`，已列入待办。）

⚠️ **改模型 ≠ 改数据库** —— 只加 `unique=True` 而不生成迁移，约束不会生效。实测 `SHOW INDEX FROM token` 确认过：只改模型时库里仍只有那个非唯一的 `user_id` 索引。

**⑤ 异常文案按约束分流**（`tools/exceptions.py`）

原实现：
```python
if "username_UNIQUE" in error_msg or "Duplicate entry" in error_msg:
    message = "用户名已存在"
```
实测 `SHOW INDEX FROM user` 只有 `PRIMARY(uid)` 和 `email` —— **`username_UNIQUE` 这个索引在库里根本不存在**，前半段永远不命中；而 `or` 的后半段把所有重复键冲突都吞成了"用户名已存在"。于是重复注册邮箱、重复点赞、重复收藏、并发建会话、并发登录，用户看到的**全是"用户名已存在"**。这不是"偶尔说错"，是从来没说对过。

改法：解析 MySQL 报错里的 `for key '表名.索引名'`，按 17 条对照表分流（邮箱/用户名/点赞/收藏/会话/消息/通知/板块/等级/token/图片），解析不出才回落"数据冲突，请检查提交的内容"。**实测 MySQL 8.0.34 的格式是 `(1062, "Duplicate entry 'x' for key 'categories.name'")`**，5.7 不带表名，所以对照表里表名和约束名都列了。

改的过程中自己写出两个 bug、跑测试才发现：①**对照表顺序错了** —— `messages.uq_message_conversation` 字符串里含 `conversation`，而 `conversation` 那条排在前面，于是"同秒重复消息"被误判成"会话已存在"（含相同子串的匹配项，更具体的必须排在前面）；②外键分支顺带改成 `lower()` 判断 —— MySQL 报错前半句 `a foreign key constraint fails` 是**小写**，大写 `FOREIGN KEY` 只出现在后半句的约束定义里，原实现靠"恰好有后半句"命中，报错被截断或换版本就漏判。

**⑥ `images.filename` 补 `unique=True`**（`models/model_image.py`）

库里本来就有唯一索引、模型漏写了，属于"库比模型严"的漂移，会导致 autogenerate 每次都想去 `drop_index` 把它**删掉**（那是功能回归）。**漂移的修法方向取决于"这个约束想不想要"**：想要 → 补模型；不想要 → 写迁移改库。无脑跟着 autogenerate 走，它会一律往"模型"的方向拉，删掉不该删的东西。

**⑦ `UserUpdateModel` 的 `username` / `nickname` 改 `Optional[str]`**（`schemas/user.py`）

注解是 `str`（不含 None）而默认值是 None，Pydantic v2 实测是"两副面孔"：**不传该字段**走默认值（默认值不参与校验）→ 通过；**显式传 `null`** → 422 `Input should be a valid string`。而 `ProfileView.vue` 传的是 `editForm.username || null`，用户清空用户名/昵称再保存就会撞上第二条。`gender: str = None` 保持原样是有意为之：那个字段前端来自 select（值恒为 男/女/未知），而库里是 `Enum('男','女','未知') NOT NULL`，放开成 Optional 反而让 None 穿过校验、最后在数据库层炸成 400。

---

#### 本次踩坑：`alembic autogenerate` 与迁移的半执行状态

本想只加一个 `token.user_uid` 唯一约束，`--autogenerate` 却生成了 **17 条操作，只有 1 条是目标**，其余全是"模型改过、但历史上没生成迁移"积累的漂移（改主键类型、删唯一索引、十几条注释变更）。直接跑在 `goods.classify` 上炸：

    (3780) Referencing column 'classify' and referenced column 'id'
           in foreign key constraint 'fk_goods_classify' are incompatible.

因为 autogenerate 只改**引用方** `goods.classify` → INTEGER，而被引用方 `goods_classify.id` 还是 varchar(36) —— MySQL 要求**外键两端类型严格一致**（正确顺序是 drop FK → 改被引用列 → 改引用列 → 重建 FK，那是独立的一件事，已列入待办）。**教训：autogenerate 生成的是「模型与数据库的全部差异」，不是「你这次想要的那个改动」，产物必须人工裁剪后再执行。**

**第二个坑：MySQL 的 DDL 不是事务性的**（alembic 日志里那句 `Will assume non-transactional DDL` 就是在说这个）。上一次 upgrade 的第 1 句（建 comments 索引）执行成功了、第 2 句才炸，于是**索引留在库里、`alembic_version` 却没推进** —— 重跑时第 1 句会撞 "Duplicate key name"。**迁移失败后必须检查"失败点之前的语句是否已生效"，不能假设它回滚了。**

**第三个坑**：想清理那个残留索引时又报

    (1553) Cannot drop index 'ix_comments_post_parent_time': needed in a foreign key constraint

查 `SHOW CREATE TABLE comments` 才看清：该索引的首列 `post_id` 正被外键 `comments_ibfk_4` **征用** —— MySQL 允许外键复用**复合索引的最左前缀**，所以这个"看起来只是性能优化"的索引其实是外键的支撑索引。

**最终解法是幂等迁移**：用 `op.get_bind()` 查 `information_schema.STATISTICS`，存在就跳过、不存在才建 —— 本地已生效的不受影响，服务器（没有这个索引）照常创建，既不用删外键重建、也不留冗余索引。

**④ 的迁移最终裁剪为 5 条操作**：comments 复合索引（幂等）、goods.update_time 索引（幂等）、messages / notifications 唯一约束（模型有、库里没有，已实测无重复数据）、token.user_uid 唯一约束。

---

#### 本次部署清单（后端 + 迁移 + 前端，三样都要）

**1. 后端** —— 拉代码后重启服务。本次 `crud/`、`models/`、`schemas/`、`tools/` 四个目录都有文件被改，别漏传。

**2. 数据库迁移（必须）**
```bash
alembic upgrade head
```
应该看到 `d1e2f3a4b5c6 -> 6f75ba267425`。验证：
```sql
SHOW INDEX FROM token;              -- user_uid 那条应变成 Non_unique=0
SHOW INDEX FROM goods;              -- 应多出 ix_goods_update_time
SHOW INDEX FROM messages;           -- 应多出 uq_message_conversation
```

**3. 前端（必须重新 build）** —— 本次改了 11 个 .vue 并新增 1 个 util 模块，不重新构建的话错误提示仍是旧的。
```bash
cd frontend && npm run build
```
⚠️ **本项目的 `vite.config.js` 设了 `build.emptyOutDir: false`，dist 不会自动清** —— 构建后确认 `dist/assets/` 里只剩下 `index.html` 实际引用的那一组 js/css，把旧 hash 文件删掉再上传，否则全量覆盖会把历史产物一起带到服务器。


