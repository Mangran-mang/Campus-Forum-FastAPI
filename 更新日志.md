#### 5/??? 所有接口完成测试可以正常使用了

#### 5/29 加入了使用langchian框架做的测试agent,虽然它没什么用

#### 6/13 

1. posts模型中token不再从前端获取而是后端直接传，新加了置顶判断

2. user模块中的路由函数，全部新增本人与管理员认证

3. crud/posts函数中，引用了or_模块来根据用户显示帖子

   优化了筛选帖子的代码，新增了板块筛选

4. 新增了帖子板块orm模型，并为posts的orm模型新增外键关联至板块id

5. 新增板块创建与更新的pydantic模型

6. 新增了板块(category)的crud函数模块，内有获取、通过id获取、新增、更新、删除模块

   等逻辑，并应用到路由

7. 更新挂载的路由

8. 评论orm模型中，评论新增父评论关联，并添加作者、父评论、依赖映射，同时用户orm模型

   中也关联了自己的所有评论

   pydantic模型中也新增父评论id的写入

   评论的crud模块中，增加父评论校验，通过帖子id返回评论改为只返回一级评论即父评论

9. 新增了点赞、收藏、通知等功能，并完善模型、crud函数、路由函数等内容

​		

#### 6/14

1. 修复了路由函数中注册时检测是否已有账号与通过邮箱查找账号冲突的bug

#### 6/15

1. 移除了头像选择，以及图片上传，遗憾哦
2. 修复了帖子和评论不正常加载头像的问题(删了就是修好了)
3. 修复了用户昵称不正常加载的问题
4. 使个人信息可以正常编辑

#### 6/16

1. 前端做了手机端适配
2. 更换bcrypt版本为4.1
3. 成功从零部署到了服务器

#### 7/6

1. 新增二手交易模块，商品分为多种类型，需要在添加时由用户自行决定类别，且商品会长久存在
2. 添加了商品表、商品分类表，并将商品与用户关联
3. 为商品页添加了评论功能
4. 经过多次更新，管理员的权限越加丰富嘞！

#### 7/14

1. 更新nginx，使其可以正确拿到图片
2. 商品可以展示图片了
3. 修复了alembic数据迁移的问题
4. 管理员可以删除任何信息了
5. 前后端新增了管理员的验证

#### 7/15

1. 根据大家的建议新增了等级系统

#### 7/28

从今天开始，未来所有除了前端的内容更新，都会写出详细的相对路径，更新了什么更新了哪个位置，为什么要更新

#### 7/30

/models/model_bookmark用变量__table_args__添加表级约束

#### 8/3

/schemas/user为email限制类型为EmailStr

/crud/image中file.content_type和PILImage.open()双重防护病毒内容，准确放行图片

/crud/image中实现压缩、识别、放进内存、取出，并在crud函数里二次验证

/crud/image使用pathlib实现灵活上传图片，既可以是商品也可以是帖子

/routers/posts修改了report_post的提交逻辑，将循环遍历管理员提交举报贴，改成了加入列表后统一由add_all提交，仍然高度解耦

/frontend前端页面改为了涂鸦风

#### 8/4

/models/conversation新的私信表

/models/model_user.py做了反向关系映射

/models/model_message.py私信内容表

#### 8/13

/config/redis_config.py为add_jti_to_blocklist增加可选expiry参数，默认仍为1小时，用于按token剩余寿命设置黑名单过期时间

/routers/user修复登出拉黑不完整的漏洞：原实现只拉黑access token的jti，refresh token仍可换新token绕过登出，现改为同时拉黑access与refresh token的jti，并删除数据库中该用户的token记录，让黑名单真正生效

/routers/user登出时黑名单TTL改为按token剩余寿命计算（access约1小时、refresh约2天），使黑名单覆盖token完整生命周期，既不漏拉黑也不浪费缓存空间

#### 8/14

/frontend/src/views/PostDetailView删除帖子详情页"编辑于xx"的显示，该时间因浏览行为被不断刷新，失去编辑时间的含义

/models/model_posts.py移除updated_time字段的onupdate自动刷新机制，避免浏览帖子（浏览量+1触发的update）污染更新时间

/crud/posts.py更新帖子时手动设置updated_time为当前时间，使该字段只在真正编辑时更新，语义回归正确

/ai_agent/review.py新增AI帖子审核模块：独立于闲聊版小柯的审核模型实例（temperature=0、max_tokens=2048），审核prompt只包含帖子标题与正文，输出JSON判定结果，兼容markdown包裹格式，解析失败兜底为未违规避免误删

/config/redis_config.py新增try_report_deduplicate举报去重函数，使用SET NX EX原子操作，24小时内同一用户不能重复举报同一帖子，Redis异常时放行保证功能可用

/routers/posts.py举报接口接入AI审核：同步调用AI审核帖子内容，判定违规则硬删帖子并通知作者与举报人，未违规保留并通知举报人，AI调用失败降级为通知管理员人工审核

/ai_agent/千问小柯.py、/ai_agent/agent_tools.py、/routers/agent.py、/schemas/talk_content.py删除无用的langchain闲聊测试agent及其关联模块，main.py同步移除agent路由挂载与导入

/frontend/src/components/PostCard.vue帖子列表卡片的举报按钮同步升级为AI审核版：增加"AI审核中"loading状态，审核完成后弹窗展示违规/未违规结果，违规则跳回帖子列表，与详情页行为一致

/frontend/src/views/PostDetailView.vue与PostCard.vue统一uid比较方式（转字符串），避免数字/字符串类型不一致导致作者与举报按钮显示误判

/frontend/src/views/PostDetailView举报按钮增加"AI审核中"loading状态，审核完成后弹窗展示判定结果，违规删帖后跳回帖子列表

#### 8/16 - 8/17

将轮询版私信改造为 WebSocket 实时通讯（项目收尾）

/tools/connection.py新增ConnectionManager连接管理器，用内存字典{uid: [websocket对象列表]}记录在线用户连接，提供connect/disconnect/send_to_user三个方法，其中disconnect为同步函数（只操作内存字典），其余为async

/routers/websocket.py新增WebSocket端点/ws/chat/{conv_id}：从URL查询参数取token并手动解码验证，查会话推导对方uid，登记连接后进入while循环收发消息，落库后信号同时推给双方，断开时清理连接

/frontend/src/views/ChatView.vue将setInterval轮询（每2分钟）替换为WebSocket连接：发送消息走ws.send，接收走ws.onmessage收到信号后刷新消息列表，onUnmounted关闭连接

/frontend/vite.config.js新增/ws代理配置（target为ws://127.0.0.1:8000），让前端WebSocket请求转发到后端

/main.py挂载websocket路由，导入方式改为圆括号换行（Python隐式换行语法）